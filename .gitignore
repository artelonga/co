/target
.DS_Store
*.swp
*.swo
.co/

# Claude Code local session state — should never be tracked
.claude/

# Runtime data directories
data/
co-web/data*/

# Database files (runtime state — never commit)
*.db
*.redb

# Environment secrets
.env
.env.local
.env.*.local

# User spaces (project namespaces) - do not commit to co home
# These are created via `co init <name>` and should live in their own repos
monica/

# Playwright / Node.js (co-web E2E tests)
node_modules/
test-results/
playwright-report/
playwright/.cache/

# Parallel execution worktrees
.worktrees/

# Runtime content (cloned at boot, NOT committed)
co-web/seed-co/
co-web/data/
/quilombo/
co-web/quilombo/
!co-web/src/quilombo/
data/

# Local backup artifacts (scripts/backup-prod-local.sh).
# Default destination is ~/co-backups/ (outside repo) but ignore in-tree
# paths defensively — prod tarballs include private universes (rfq).
backups/
co-backups/
*.tar.gz

# MaxMind GeoLite2 data — required attribution, large binary
GeoLite2-*.mmdb

# CO-243: LSP rust build artifacts
co-lsp/target/
{,/}data-test-co305/

# CO-375: contract probe report (generated artifact — not committed)
contract-probe-report.json
