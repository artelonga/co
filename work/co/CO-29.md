---
id: 29
title: "CodeMirror 6 editor — markdown editing with live preview, open to all"
status: done
priority: critical
parent: 20
labels:
  - mvp
  - editor
  - frontend
module: co-web
created_at: 2026-04-06T00:00:00Z
updated_at: 2026-04-06T16:08:48.941233+00:00
---

GIVEN tasks and content currently use plain textarea for editing,
WHEN I integrate CodeMirror 6 as the markdown editor for all content,
THEN:

- [ ] Add build step: `co-web/editor/` directory with `package.json`, esbuild config
- [ ] Dependencies: `@codemirror/state`, `@codemirror/view`, `@codemirror/lang-markdown`, `@codemirror/language-data`, `@lezer/highlight`
- [ ] Bundle output: `co-web/static/shared/editor.bundle.js` (tree-shaken, single file, embedded via rust-embed)
- [ ] `initEditor(container, { content, onChange, readOnly })` — public API function
- [ ] Markdown mode with GFM extensions (tables, task lists, strikethrough, code blocks with language highlighting)
- [ ] Split-pane layout: left = CodeMirror, right = rendered HTML preview (using marked, sanitized with DOMPurify)
- [ ] YAML syntax highlighting in frontmatter section (between `---` delimiters)
- [ ] Theme tokens: CodeMirror theme reads from CSS custom properties (`--bg`, `--text-primary`, `--accent`, etc.) so it respects the active palette
- [ ] Keyboard shortcuts: Ctrl/Cmd+B = bold, Ctrl/Cmd+I = italic, Ctrl/Cmd+K = link, Ctrl/Cmd+S = save
- [ ] Toolbar: bold, italic, heading, link, code, list, task list, preview toggle
- [ ] Editor used in: task description field (modal), project description, any content body
- [ ] No login required — editor is available to everyone including anonymous universe owners
- [ ] Mobile: preview hidden by default (< 768px), toolbar simplified
- [ ] Lazy load: editor bundle loaded on first edit action, not on page load
- [ ] Test: open task modal → CodeMirror initializes → type markdown → preview updates → save persists
- [ ] `cargo test` passes, `cargo clippy -- -D warnings` clean
