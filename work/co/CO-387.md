---
id: 387
title: "Time-rendering primitive — calendar/timeline lens system (canonical ms, pluggable render)"
type: user-story
status: todo
priority: high
conventional_commit: "feat(time):"
semver_bump: minor
labels:
  - type:feat
  - module:time
  - module:render
  - module:spa
module: co-web
parent: 0
created_at: 2026-06-06T00:00:00Z
updated_at: 2026-06-06T00:00:00Z
related:
  - 73    # CO-73 — temporal model (event_at/due_at/scheduled_at)
  - 355   # workspace template registry (pattern: per-universe YAML config)
  - 380   # event bus (time-rendering subscribes to entry events for live updates)
  - 381   # live timeline (uses this primitive)
---

## As
Yuri organizing work with a 4-day-week experiment, AND viewing the Milky Way → human history → fictional epochs side-by-side at `/timeline`

## I Need
A time-rendering primitive that decouples canonical time storage (Unix ms) from rendering. A per-universe `_calendar.yaml` defines available calendar lenses; the UI lets users switch lenses at view time without touching the underlying entries.

## So That
- The IaaS principle holds for time: bounded service (lens spec) renders the brain content (entries with timestamps) — same pattern as themes for design, workspaces for layout.
- A 4-day-week org review doesn't require a schema migration — same entries, different lens.
- Cosmic / human / fictional timelines render with the same component, with different epoch + scale.
- The brain's creative time (custom calendars, fictional epochs) is liberated; the service's time (timestamps, durations) stays bounded + deterministic.

## Context

CO entries already have `event_at`, `due_at`, `scheduled_at` per CO-73 (shipped). They store as ISO 8601 strings today. This spec:
1. Migrates them to canonical Unix ms (or stores both, with ms as authoritative).
2. Adds a per-universe `_calendar.yaml` defining available lenses.
3. Builds a `<co-time-grid>` SPA component that accepts `(entries, lens)` → renders.

The "render different calendars" idea isn't theoretical. Use cases listed:
- **4-day-week org review** — visualize a quarter's work as 91 weeks × 4 days = 364 day-slots; see whether the team's rhythm fits.
- **Cosmic + human + fictional timeline** — `/timeline` at CO renders Milky Way events (epoch: Big Bang) alongside human history (epoch: CE) alongside a fictional universe (epoch: arbitrary), each lens defining its own scale (log for cosmic, linear for human).
- **Pomodoro view** — 25-minute work cells with 5-minute breaks.
- **Fiscal year view** — Apr-Mar instead of Jan-Dec.
- **Lunar calendar** — for cultural content (Islamic, Chinese, Mayan).

## Design

### Canonical storage

Migration v66 normalizes timestamps to Unix ms:

```sql
ALTER TABLE entries ADD COLUMN event_at_ms INTEGER;
ALTER TABLE entries ADD COLUMN due_at_ms INTEGER;
ALTER TABLE entries ADD COLUMN scheduled_at_ms INTEGER;

-- Backfill from existing ISO strings
UPDATE entries
SET event_at_ms = strftime('%s', event_at) * 1000
WHERE event_at IS NOT NULL AND event_at_ms IS NULL;
```

Old ISO columns kept for backward compat (computed on read).

### Per-universe `_calendar.yaml`

```yaml
default_lens: gregorian

lenses:
  - id: gregorian
    name: Gregorian (canonical)
    epoch_ms: 0                       # Unix epoch
    scale: linear
    week_length_days: 7
    weekday_names: [Mon, Tue, Wed, Thu, Fri, Sat, Sun]
    month_length: gregorian           # special: variable per month
    timezone: America/Sao_Paulo

  - id: 4-day-week
    name: 4-day week experiment
    epoch_ms: 1735689600000            # 2025-01-01 BRT (project start)
    scale: linear
    week_length_days: 4
    weekday_names: [Um, Dois, Três, Quatro]
    display_format: "S{week}.D{day}"   # Sprint.Day

  - id: cosmic
    name: Cosmic (Big Bang → now)
    epoch_ms: -435296256000000000      # 13.8 Gya
    scale: log
    display_unit: "billion years"
    label_periods:
      - { name: "Big Bang", at_ms: -435296256000000000 }
      - { name: "Sun forms", at_ms: -141912000000000000 }
      - { name: "Earth forms", at_ms: -141750000000000000 }
      - { name: "First life", at_ms: -113400000000000000 }
      - { name: "Common Era", at_ms: -62135596800000 }

  - id: fictional-mythos
    name: Shandara epoch
    epoch_ms: 0                        # arbitrary; uses internal `mythos_year` field
    scale: linear
    custom_event_field: "mythos_year"  # entry.payload.mythos_year drives position
    week_length_days: 7

  - id: pomodoro
    name: Pomodoro (work cells)
    epoch_ms: 1735689600000
    scale: linear
    cell_duration_ms: 1500000          # 25 min
    break_duration_ms: 300000          # 5 min break
    display_format: "Pom{cell}"
```

Loader: `co-web/src/time/calendar_loader.rs`. Universe without `_calendar.yaml` defaults to Gregorian (no breaking change).

### Render component

`<co-time-grid>` SPA component (vanilla JS, no framework):

```html
<co-time-grid
  universe="co"
  lens="4-day-week"
  range-from="2026-01-01T00:00:00Z"
  range-to="2026-12-31T23:59:59Z"
  group-by="entry_type"
  view-mode="grid">
</co-time-grid>
```

Modes: `grid` (calendar grid), `timeline` (horizontal bars), `scatter` (events as dots), `gantt` (with durations).

Subscribes to event bus (CO-380): live updates as entries change in the rendered range.

### Lens-switching UX

Universe header gets a lens dropdown next to the language toggle:

```
[ 🗓 Gregorian ▾ ]  [ pt-BR ▾ ]
   ├─ Gregorian (canonical)
   ├─ 4-day week experiment
   ├─ Cosmic
   ├─ Shandara epoch
   └─ Pomodoro
```

Selected lens stored in `localStorage.co_calendar_lens_<universe>`. Switching = re-render only; no API call.

### Conversion math

Pure functions in `co-web/src/time/conversion.rs` (mirror in `static/.../lib/co-time.js`):

```rust
// Unix ms → lens-specific position
pub fn ms_to_lens_position(ms: i64, lens: &Lens) -> LensPosition {
    match lens.scale {
        Scale::Linear => {
            let offset = (ms - lens.epoch_ms) as f64;
            let day = (offset / 86_400_000.0).floor() as i64;
            let week_len = lens.week_length_days as i64;
            LensPosition {
                week: day / week_len,
                day_of_week: day % week_len,
                hour: ((offset / 3_600_000.0).floor() as i64) % 24,
            }
        }
        Scale::Log => {
            // For cosmic — log_10 of years before now
            let years = (ms - 0) as f64 / 31_557_600_000.0;
            LensPosition { log_position: years.abs().log10(), ..default }
        }
    }
}
```

Reverse: `lens_position_to_ms` for "click on a cell to create event there".

### Timeline at /timeline (the killer use case)

`/timeline` becomes a multi-universe time view (existing alias per CO-280):
- Renders entries from `tempo`, `universo`, `humanity` universes
- Each entry tagged with origin universe → color-coded
- Lens dropdown: switch between cosmic / human / fictional
- Cosmic lens: log scale, Big Bang → now, milky way events visible
- Human lens: linear, CE → now, with cultural overlays
- Fictional lens: arbitrary epoch (per `_calendar.yaml`'s mythos)

User can pin multiple lenses side-by-side via `?lens=cosmic,human,fictional` → renders 3 stacked rows with same time range. The Brain compares mentally.

### Privacy

CO-378 path redaction applies: private universes' entries don't appear in the rendered grid for anon viewers. Calendar lens metadata is universe-scoped — choosing a lens never reveals other universes' data.

### Telemetry (via CO-380)

- `time.lens_switched{universe, from_lens, to_lens}` per user choice
- `time.grid_rendered{universe, lens, entry_count}` per load (sampled)
- Visible in /agora live timeline

## Acceptance

- [ ] Migration v66 adds `event_at_ms`/`due_at_ms`/`scheduled_at_ms` + backfills
- [ ] `_calendar.yaml` loaded per universe; default = Gregorian if absent
- [ ] `<co-time-grid>` component renders 4 view modes (grid, timeline, scatter, gantt)
- [ ] Lens dropdown switches without reload
- [ ] 4-day-week lens correctly groups entries into 91 weeks × 4 days for a year
- [ ] Cosmic lens (log scale) places Big Bang at one edge, Common Era near the other
- [ ] `?lens=A,B,C` pins multiple lenses stacked
- [ ] localStorage persists lens choice per universe
- [ ] Live updates: entry created → grid updates within 300ms (via CO-380 bus)
- [ ] Pomodoro lens groups by 25-min cells with 5-min breaks
- [ ] Custom `mythos_year` field drives Shandara/fictional lens
- [ ] No regression in CO-73 existing temporal queries
- [ ] OpenAPI documents the `_calendar.yaml` shape
- [ ] `docs/time-rendering.md` describes the primitive

## Blast radius

- 1 migration (v66) + backfill
- ~250 lines `co-web/src/time/{calendar_loader, conversion}.rs`
- ~400 lines `<co-time-grid>` SPA component + library
- ~150 lines lens loader + dropdown UI
- ~100 lines `/timeline` route extension for multi-lens stacking
- ~200 lines unit + integration tests
- 1 new docs file
- 0 breaking changes (ISO columns kept)

## Dependencies

- CO-73 ✓ shipped — temporal model
- CO-280 ✓ shipped — `/timeline` alias exists
- CO-355 (Wave 4) — `_workspace.yaml` pattern reused for `_calendar.yaml`
- CO-380 (Wave 4) — event bus for live updates
- CO-381 (Wave 4) — live timeline (overlapping rendering primitive; may share lib)

## Not in scope

- **AI-suggested lens** (auto-pick best lens for content) — defer
- **Lens authoring UI** (visual builder) — start with YAML; editor later
- **3D timelines** (cosmic with depth) — defer
- **Audio scrubbing** along the lens — defer
- **Per-event lens override** (entry has frontmatter saying "render me in lens X") — defer
- **Inter-lens conversions on the same entry** (show Pomodoro cell labels on Gregorian grid) — defer
- **Recurrence rules** (RRULE iCal) — separate spec
