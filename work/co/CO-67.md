---
id: 67
title: "Prod universe seed — artelonga, quilomboaraucaria, rfq with content"
status: done
priority: high
parent: 20
labels:
  - operations
  - deploy
  - content
module: ops
created_at: 2026-04-26T00:00:00Z
updated_at: 2026-04-30T08:49:22Z
shipped_in: 1.20.x
---

> **Done.** `scripts/seed-prod-universes.sh` (jj-tracked delta upload, keychain-stored API token via `co-token`) seeded artelonga (146 entries), rfq, and qa-dev to prod. quilomboaraucaria came in via the legacy migration binary. Subsequent `cleanup_admin_anon_clutter` (1.21.1) finished the cleanup. CO-100 doc pass will document the procedure for re-runs.

GIVEN the manifest epic needs real universes with real content to validate against, and three production targets are identified (`artelonga`, `quilomboaraucaria`, `rfq`),
WHEN we seed them on prod with current content from local folders,
THEN CO-63 has live data to test manifest-driven rendering against.

## Tasks

- [ ] Promote `/tmp/bulk-upload.py` to `co/scripts/seed-universes.py` (committed, with bounded concurrency)
- [ ] Login to prod via email-code flow as yuri@artelonga.com.br
- [ ] `POST /api/v1/universes` for `artelonga` (private — staying private per owner's call)
- [ ] Verify `quilomboaraucaria` already auto-seeded as public-subscribable on prod
- [ ] `POST /api/v1/universes` for `rfq` (private)
- [ ] Run bulk upload from `~/projects/ArteLonga`, `~/projects/quilomboaraucaria`, `~/projects/rfq-gateway`
- [ ] Verify `content_count` per universe matches local file counts
- [ ] After CO-65 ships, optionally restore `quilomboaraucaria` description to user-set value

## Scale notes

- Bulk upload script today is sequential — fine for hundreds of files; for thousands, parallelize with `asyncio.Semaphore(10)` or similar bounded concurrency
- Vault API rate limit is 60 req/min per token (CO-35) — script must add token-bucket throttling for large universes; will be relaxed by CO-80
