---
id: 401
title: "Staging fixtures + CO_STAGING_ADMIN_TOKEN — unlock the deep staging suite (3949 skips → coverage)"
type: task
status: todo
priority: high
conventional_commit: "chore(ci):"
semver_bump: patch
labels:
  - type:chore
  - module:ci
  - module:platform
module: co-web
parent: 374
created_at: 2026-06-10T00:00:00Z
updated_at: 2026-06-10T00:00:00Z
related:
  - 374   # the staging suite these fixtures unlock
  - 379   # staging app
---

## As

the release gate, I need staging seeded with the fixtures the CO-374 suite
preconditions on (recursion-a/-b/-c universes, funnel test data) and a scoped
admin token in CI secrets, so the Thursday gate exercises real scenarios
instead of skipping 3949 tests.

## Acceptance

- [ ] Boot-time staging seeder (CO_ENV=staging only) creates recursion-a → recursion-a-b → recursion-a-b-c chain idempotently
- [ ] Funnel/lead fixture data seeded (marked synthetic, excluded from analytics rollups)
- [ ] `CO_STAGING_ADMIN_TOKEN` issued as a **CO-448 capability-scoped** token (NOT admin-tier NULL-scope); documented rotation in docs/cross-env-auth.md
- [ ] staging-suite workflow passes the token; suite skip count drops below 500
- [ ] Fixtures never seed in production (env-gated; test asserts the gate)

> **REWORK (2026-06-14) — least-privilege, sobre CO-448.** A 1ª versão (#246,
> held/draft) emitia um token **admin-pleno** (NULL-scope inherit-tier); o dono
> exigiu **least-privilege**. CO-448 (merged; main em **v81**) trouxe `scopes`
> em `api_tokens` + guard `requires_admin_to_grant`. Esta task agora:
> - **Rebase em main** (já tem CO-448). Migração desta task: **claim v82**
>   (`v082.rs`, `migrate_v082`, `if current_version < 82`) — CO-448 levou v81.
> - `seed_staging_admin_token` emite um **token escopado** com o **mínimo** que a
>   suíte CO-374 exercita. O usuário `staging-admin` continua **admin-tier**
>   (preciso pra *mint* capacidades admin), mas o **token** carrega `scopes`
>   explícitos (ex.: bundle `admin`, ou subconjunto `entries:read/write,
>   universes:read/write, gestao:read, funnel:read, chat:read, telemetry:read`
>   conforme o que CO-374 toca) — **não** NULL/inherit. Vazamento → só o escopo.
> - Reusar o resto (seeder de fixtures, funnel, `staging-suite.yml`, docs) — só o
>   **token vira escopado** + migração v82.
> - Teste: token escopado alcança as superfícies admin da suíte (via capacidade)
>   e **não** alcança fora do escopo.
