---
title: "Manifest visibility + workspace reconcile + code-repo skip"
status: done
---

# CO-467 — Manifest visibility + workspace reconcile + code-repo skip

Follow-ups to CO-466: fix the manifest↔DB visibility drift and stop CO-466
over-registering dormant code repos as universes.

## Done
- `Manifest` parses optional `visibility`; scan honors it (was hardcoded private).
- Existing workspace rows reconcile visibility/is_public/parent on re-scan.
- Pure code repos (code manifest + no `_universe.yaml` + no `content/`) skipped.
- `parent`/`surface_dns`/`visibility` registered as known top-level manifest keys.

## Acceptance
- [x] Manifest `visibility` honored on register (public-* → is_public).
- [x] Folder without manifest defaults to `private`.
- [x] Editing manifest visibility + re-scan reconciles the existing row.
- [x] Pure code repo (Cargo.toml + README, no manifest, no content/) is skipped.
- [x] `cargo test` (scan + manifest) green; `cargo clippy -- -D warnings` clean.
