---
id: 415
title: "Login com GitHub em co.artelonga.com.br (OAuth) — pedido do Miguel"
type: user-story
status: done
priority: high
conventional_commit: "feat(auth):"
semver_bump: minor
labels:
  - type:feat
  - module:collab
module: co-web
parent: 414
created_at: 2026-06-12T00:00:00Z
updated_at: 2026-06-12T12:00:00Z
related:
  - 177   # oauth_google.rs — o padrão OAuth a espelhar
  - 230   # auth unification epic
---

## As
Miguel (e qualquer visitante), QUERO entrar no co.artelonga.com.br com minha
conta do GitHub, PARA não criar mais uma senha.

## Contexto técnico
Hoje só existe `oauth_google.rs` (login Google, full OIDC code-flow) e
`github_auth.rs` (verificação de PAT de admin — NÃO é login). Esta story
adiciona o OAuth-login do GitHub espelhando exatamente o padrão Google.

## Acceptance Criteria
- `GET /api/v1/auth/github/start?return_to=` redireciona ao consent do GitHub
  (state JWT assinado: return_to + nonce + exp, mesma postura do Google).
- `GET /api/v1/auth/github/callback?code=&state=` troca code→token, busca
  `GET /user` + email verificado, find-or-create por `users.github_login`
  (nova coluna) e cai pra email, seta cookie de sessão, redireciona ao
  return_to safelisted.
- Env: `GITHUB_OAUTH_CLIENT_ID` / `GITHUB_OAUTH_CLIENT_SECRET` (distintos do
  PAT de admin); endpoints retornam 503 quando ausentes; botão "Entrar com
  GitHub" some quando não-configurado.
- email não-verificado → 401. Reusa `maybe_attach_co_handover_token` (CO-186).
- Teste de integração do round-trip state + find-or-create.

## Acceptance
- [x] `GET /api/v1/auth/github/start?return_to=` redireciona ao consent do
      GitHub (scope `read:user user:email`) com state JWT assinado (return_to +
      nonce + exp + origin, `return_to` safelist-checked) — mesma postura do
      Google.
- [x] `GET /api/v1/auth/github/callback?code=&state=` troca code→token
      (`Accept: application/json`), busca `GET /user` + `GET /user/emails`,
      seleciona o email primário verificado, find-or-create por
      `users.github_login` (nova coluna, migração v74) caindo pra email, seta
      cookie de sessão e redireciona ao return_to safelisted.
- [x] Env `GITHUB_OAUTH_CLIENT_ID` / `GITHUB_OAUTH_CLIENT_SECRET` (distintos do
      PAT de admin do `github_auth.rs`); ambos endpoints retornam 503 quando
      ausentes; botão "Entrar com GitHub" some via `/github/status` quando
      não-configurado.
- [x] email não-verificado → 401. Reusa `maybe_attach_co_handover_token`
      (CO-186) e a telemetria `auth.login` (list=`github`).
- [x] Testes: round-trip do state JWT (encode↔decode, rejeita segredo errado),
      seleção de email verificado, e find-or-create por github_login + link por
      email (`storage_tests.rs`). `cargo test -p co-web` verde, clippy limpo.
- [x] Rotas no `api-catalog.md` + openapi regenerado (`openapi:check` sem drift).

## Notas
co-auto-runnable. Status transita open→in_progress→review→done pelo quadro.
