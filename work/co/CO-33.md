---
id: 33
title: "E2E test suite — Playwright for full MVP flow"
status: done
priority: high
parent: 20
labels:
  - mvp
  - testing
  - e2e
module: co-web
created_at: 2026-04-06T00:00:00Z
updated_at: 2026-04-06T18:59:35.687313+00:00
---

GIVEN Playwright is already configured in `co-web/e2e/` with helper utilities,
WHEN I write E2E tests covering the full MVP user journey,
THEN:

### Landing & Universe Creation
- [ ] Test: visit /co → hero visible, template board renders read-only, "Criar universo" button present
- [ ] Test: click "Criar universo" → fill name/slug → submit → redirected to /co/:slug → board is editable
- [ ] Test: template board interactions disabled (no drag, no edit) with tooltip

### Board CRUD
- [ ] Test: create project → create task → drag between columns → edit task → delete task
- [ ] Test: subtask tree expand/collapse
- [ ] Test: view switcher (kanban, table, timeline, calendar, dashboard) — each renders

### CodeMirror Editor
- [ ] Test: open task edit modal → CodeMirror initializes → type markdown → preview updates in split pane
- [ ] Test: toolbar buttons (bold, italic, heading) insert correct markdown syntax
- [ ] Test: save → content persists → reopen → content intact

### Usage Gate
- [ ] Test: anonymous user creates 100 entries → 101st shows "Crie conta" modal
- [ ] Test: login after gate → can continue creating entries

### Theme & Form
- [ ] Test: palette switcher shows 4 palettes for anonymous, all for logged-in
- [ ] Test: switch theme → CSS variables update → no page reload
- [ ] Test: universe owner changes theme in settings → visitor sees updated theme

### i18n
- [ ] Test: toggle language pt→en → all UI strings change → reload → persists via cookie

### Auth & CRDT
- [ ] Test: login flow (email code) → session cookie set → protected routes accessible
- [ ] Test: anonymous universe → no shareable link → login → link works for visitors
- [ ] Test: two logged-in browser contexts edit same doc → CRDT syncs changes bidirectionally
- [ ] Test: anonymous user opens editor → no WebSocket connection (local only)

### Responsive
- [ ] Test: board renders at mobile (375px), tablet (768px), desktop (1280px) viewports

### CI Integration
- [ ] GitHub Actions workflow: build co-web → start server → run Playwright → upload report
- [ ] Test fixtures: seed database with known state before each test suite
- [ ] All tests pass in CI
