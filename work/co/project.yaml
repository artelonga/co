name: CO Platform
key: CO
description: CO open source platform — board UI, API, CLI, desktop apps
created_at: 2026-04-01T00:00:00Z
next_id: 439

# CO-427 model routing + CO-425/426 usage reporting.
# usage_endpoint lights up the prod /usage dashboard (CO-426): every co-auto run
# in this space POSTs its token/cost/tool-usage here. The bearer for the POST is
# supplied at launch via CO_SESSION_TOKEN env (secret — never commit it).
# by_priority stays false → quality-first Opus default unless `model:` per task.
routing:
  usage_endpoint: https://co.artelonga.com.br
