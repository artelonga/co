---
assistant_messages: 42
ended_at: 2026-04-02T00:00:43.934Z
project: -Users-artelonga-projects-co
session_id: 58a1c1e1-9a5e-4aa1-8470-adbe1b46fffd
started_at: 2026-04-01T23:28:57.828Z
title: co-auto-CO-7
type: claude-session
user_messages: 22
---

# co-auto-CO-7

## User · 2026-04-01T23:28:57

YOUR TASK: CO-7 — Auth-protect board write operations

Read .claude/co-auto-context.md for full context. Look for the section '## Current Task: CO-7' — that contains your acceptance criteria.

IMPORTANT: Only implement CO-7. All dependencies are already merged into main. Do NOT re-implement any other task. Each `- [ ]` item is a required deliverable. Commit when all criteria are met.

## Assistant · 2026-04-01T23:29:07

Now let me read the relevant source files.

## Assistant · 2026-04-01T23:29:26

Now let me check the existing tests:

## Assistant · 2026-04-01T23:29:48

Now I have a clear picture. I need to:
1. Split the `api` router in `server.rs` into public GET routes and protected write routes
2. Update `concurrency_tests.rs` to include JWT tokens in write requests

Let me implement this:

## Assistant · 2026-04-01T23:30:09

Now update the concurrency tests to include JWT tokens for write operations:

## Assistant · 2026-04-01T23:30:17

Now add auth headers to all POST requests in the tests. There are write requests in `test_concurrent_task_creation` and `test_concurrent_read_write`:

## Assistant · 2026-04-01T23:30:26

Now fix the seed writes in `test_concurrent_read_write`:

## Assistant · 2026-04-01T23:30:43

Now let's run the tests to verify everything works:

## Assistant · 2026-04-01T23:30:58

There's also an `api_tests.rs` file. Let me read it:

## Assistant · 2026-04-01T23:31:14

I need to add a `test_bearer()` helper and auth headers to all write operations in `api_tests.rs`. There are many requests to update — I'll rewrite the file:

## Assistant · 2026-04-01T23:32:47

All 35+2+23 tests pass. Now let's run clippy and fmt:

## Assistant · 2026-04-01T23:32:55

Clean. Now commit:

## Assistant · 2026-04-01T23:33:11

Done. Here's what was implemented for CO-7:

**`co-web/src/server.rs`**:
- Replaced the single `api` router with two routers:
  - `board_public` — GET routes (list/get projects, tasks, comments, activity, dashboard, health) — no auth required
  - `board_protected` — POST/PUT/DELETE routes — wrapped with `require_auth` middleware, returns 401 JSON on missing/invalid token
- Added `delete` and `put` imports to `axum::routing`

**`co-web/tests/api_tests.rs`** and **`tests/concurrency_tests.rs`**:
- Added `test_bearer()` helper that generates a valid JWT using the dev secret
- Added `Authorization: Bearer <token>` header to all write requests
- Added new `test_write_without_auth_returns_401` test verifying the 401 response with JSON error body

All 60 tests pass, clippy is clean, fmt is clean.

## User · 2026-04-02T00:00:26

┌──────┬───────────────────────────┬─────────────────────────────────────────────────────────────┐
  │ Task │           Work            │                          Priority                           │                    
  ├──────┼───────────────────────────┼─────────────────────────────────────────────────────────────┤
  │ CO-8 │ Delete project endpoint   │ Do first — simple, unblocks cleanup                         │
  ├──────┼───────────────────────────┼─────────────────────────────────────────────────────────────┤
  │ CO-7 │ Auth-protect board writes │ Do before deploy — all mutating routes need auth middleware │                    
  └──────┴───────────────────────────┴─────────────────────────────────────────────────────────────┘                    
                                                                                                  

## Assistant · 2026-04-02T00:00:43

Both tasks in that table are already done:

- **CO-8** — `feat(board): add delete project endpoint` was committed at `b0820b2` (before this session)
- **CO-7** — `feat(board): auth-protect write operations` was just committed at `9dc0d82`

The board write operations are now protected. Next up on the roadmap is Phase 2 — the board UI work (CO-2, CO-3, CO-4, CO-5). CO-2 is also already merged (`d17352a`). The remaining ones are CO-3 (timeline), CO-4 (dashboard), and CO-5 (variant palette).
