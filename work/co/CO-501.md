---
id: 501
title: "Bilingual PT/EN garden synthesis (output A) — raw → organized markdown page with frontmatter + wikilinks"
status: todo
priority: high
parent: 20
labels:
  - feat
  - whatsapp
  - content
  - i18n
module: co-web (entries/relations + translate routes) + tools/whatsapp-bot (garden capability)
release: whatsapp-self-sovereign
created_at: 2026-06-27T00:00:00Z
updated_at: 2026-06-27T00:00:00Z
---

GIVEN output A of the synthesis loop is the **Jardim**: a raw item turned into an
organized markdown page that fits the universe's existing architecture (a `diario/`
entry, a project page, or a `pessoas/` page), carrying frontmatter and connection
wikilinks (which CO-74 lifts into typed relations), and the content should be
**bilingual PT/EN**,
WHEN the router (CO-498) sends a captura/nota/documento/pessoa item to A,
THEN the bot synthesizes it into a well-formed bilingual page — body organized,
frontmatter filled, `[[…]]` connections drawn — placed at the right path in the
existing garden, by default, with any inference marked as an assumption.

## Acceptance

- [ ] Bot **garden capability** (routed to by CO-498) that turns a raw item into an
  organized markdown page: structured body + **frontmatter** + **connection
  wikilinks** that become `entry_relations` (CO-74) on write.
- [ ] **Bilingual PT/EN:** the page carries both languages (via the existing
  `translate` routes), with PT canonical per the domain-terms convention; the
  envelope `idioma` field (CO-498) seeds the source language.
- [ ] **Fits existing architecture:** chooses/produces a path under the universe's
  real structure — `diario/`, project pages, `pessoas/` — rather than a flat dump;
  cross-universe links use `key::path` canonical form.
- [ ] Writes through the per-sender scoped token (CO-490); ambiguity recorded as an
  inline assumption, never a blocking question (execute-by-default).
- [ ] Tests: frontmatter + wikilink extraction, PT/EN pairing, path placement for
  diario/project/pessoa, relation creation. `CHANGELOG-PENDING/CO-501.md`.

## Notes
- A is the richest write output; it shares the content substrate that CO-502 renders
  live on localhost (co-web view) alongside the yggdrasil view.
- Reuse the existing translate route rather than introducing a new translation
  path; keep technical terms in English per the glossary.
