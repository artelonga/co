---
id: 22
title: "Template universe — seed data, read-only for visitors, 'Criar universo' CTA"
status: done
priority: critical
parent: 20
labels:
  - mvp
  - frontend
  - backend
module: co-web
created_at: 2026-04-06T00:00:00Z
updated_at: 2026-04-06T13:50:22.113699+00:00
---

GIVEN the universe CRUD API exists (CO-21),
WHEN I create a template universe with seed content and a clone flow,
THEN:

- [ ] Seed SQL: create template universe with `slug='template', name='CO', is_template=true, is_public=true`
- [ ] Seed projects in template: "Meu Projeto" (key: MP) with 5-8 sample tasks across all statuses (todo, in_progress, review, done) demonstrating board capabilities
- [ ] Sample tasks include: different priorities, labels, parent/subtask relationships, due dates — showcasing all board features
- [ ] Template universe is read-only: API returns 403 on write attempts to template universe for non-admin users
- [ ] Landing UI at `/co`: board renders normally but with a banner/overlay:
  - Hero text: "CO — Gestão de conteúdo em grafo" (pt) / "CO — Graph-based content management" (en)
  - CTA button: "Criar universo" (prominent, centered above board)
  - Secondary: "Entrar" link for existing users
- [ ] "Criar universo" flow: modal with name + slug fields → POST /api/v1/universes/:slug/clone (source=template) → redirect to `/co/:new-slug`
- [ ] After clone, user lands in their own universe with full edit capability
- [ ] Template universe updates don't propagate to clones (one-time copy)
- [ ] Seed runs once on first boot (check `is_template` exists, skip if so)
- [ ] Test: visit /co → see template → click criar → get own universe → can edit
- [ ] `cargo test` passes, `cargo clippy -- -D warnings` clean
