---
id: 227
title: "Epic — Server decomposition"
type: epic
status: done
priority: high
conventional_commit_default: "refactor(server):"
semver_bump_aggregate: patch
labels:
  - type:refactor
  - epic
module: co-web
created_at: 2026-05-18T00:00:00Z
updated_at: 2026-05-18T00:00:00Z
---

## Goal

Decompose the 1570-LoC `server.rs` god-file — THE unblocker for every downstream CO refactor. Once split, AppState slim, storage cycle break, route folders, and chat extraction all land cleanly inside SRP.

## Children

- CO-215 — Split `server.rs` into `server/{router, state, validation, uat_boot, seed_orchestrator}.rs`
- CO-216 — Break `storage ↔ server` import cycle via `StorageContext` trait
- CO-219 — Promote `chat_routes.rs` (2063 LoC) into `chat/` module folder
- CO-221 — Slim `AppStateInner` (18 fields) via segregated sub-states
- CO-224 — Promote routes into context folders (`auth/`, `content/`, `social/`, `admin/`)

## Acceptance

No Rust file in `co-web/src/` exceeds 500 LoC; no top-level `*_routes.rs` outside their feature folder; `cargo test -p co-web` + `cargo clippy -- -D warnings` clean throughout.
