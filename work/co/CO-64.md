---
id: 64
title: "Post-GitHub cleanup — remove dead git_sync.rs, formalize ARCHITECTURE.md"
status: in_progress
priority: high
parent: 20
labels:
  - cleanup
  - architecture
  - docs
module: co-web
created_at: 2026-04-26T00:00:00Z
updated_at: 2026-06-21T09:23:31.604346+00:00
---
> **Migration PINNED 2026-06-14: v091** (wave: tip v085; v086 CO-458, v087 CO-449, v088 CO-78, v089 CO-457?, v090 CO-454?). The dead-column DROP migration claims **v091** — re-confirm max+1 vs open PRs before writing.


GIVEN the v2.0 pivot replaces git-clone-on-server (CO-50, CO-55) with the Vault REST API as the canonical input path, and `co-web/src/git_sync.rs` is dead code on this path,
WHEN we remove the universe-as-repo plumbing and write a single ARCHITECTURE.md describing the post-GitHub model,
THEN the manifest epic (CO-63) has a clean foundation and contributors stop confusing the two paths.

## Tasks

- [ ] Delete `co-web/src/git_sync.rs` and the `GIT_DEPLOY_KEY_PATH` fallback
- [ ] Drop columns `git_repo`, `git_path`, `git_branch`, `git_commit_hash`, `git_synced_at`, `git_sync_error` from `universes` (online migration)
- [ ] Remove routes: `PUT /api/v1/universes/:slug/git`, `POST /api/v1/universes/:slug/sync`, `POST /api/v1/universes/:slug/webhook`
- [ ] Mark CO-50 and CO-55 as `status: deprecated` in their task files
- [ ] Write `co-dev/ARCHITECTURE.md`: post-GitHub data model, Vault REST API as canonical sync, forward refs to CO-77 (per-universe SQLite) and CO-70 (manifest)
- [ ] Update `co/CLAUDE.md` to remove git-deploy-key references

## Scale notes

- Removing the git-clone path eliminates the per-universe `git clone`/`git pull` shell-out, which was a serialization bottleneck on the storage mutex
- Schema migration must be online (no downtime); use ALTER TABLE DROP COLUMN with WAL checkpoint

## Acceptance

- `cargo test -p co-web` passes
- Zero references to `GIT_DEPLOY_KEY_PATH` in code, env, or docs
- `ARCHITECTURE.md` exists and is linked from `README.md`
