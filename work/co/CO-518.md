---
id: 518
title: "Wave-review fixes — Cloud-API window fallback, erase/forget contract, wamid retry, digest param (+ cross-process contract tests)"
status: done
priority: high
parent: 20
labels:
  - fix
  - whatsapp
  - lgpd
  - tests
module: co-web (notification_providers/whatsapp/lead_digest/birthday) + tools/whatsapp-bot
release: whatsapp-self-sovereign
created_at: 2026-06-28T00:00:00Z
updated_at: 2026-06-28T00:00:00Z
---

GIVEN the wave code-review found 11 verified defects in shipped work — most
critically that the LGPD erase/forget right and the Cloud-API link flow were
**non-functional** because both sides of each cross-process contract were
unit-tested with fakes (so mismatches passed CI but broke in production),
WHEN we fix each finding,
THEN every fix ships with the contract/integration test that would have caught it,
so this class of bug fails CI instead of prod.

## Acceptance

- [x] **CRITICAL** — erase/forget: bot posts the exact `{confirm: "apagar tudo"/"esquece de mim"}` co-web requires (was a random nonce → 400). `test_co_contract.py` pins the phrases against `whatsapp_me.rs` so neither side drifts.
- [x] **HIGH** — CO-496 window/template fallback wired into BOTH send paths (`link_start` OTP + birthday) via `send_business_initiated`: outside the 24h window → pre-approved template, else an actionable error (never free-form Meta rejects). Test asserts template-on-the-wire outside window.
- [x] **MED** — `wamid` recorded only after a successful enqueue; a shed message no longer poisons its own Meta retry. Shed→retry test.
- [x] **MED** — bot two-step destructive confirm has a 300s TTL; a stale "ok" can't trigger erase/forget. Test ages a pending op past TTL.
- [x] **LOW** — `/api/chat` fails closed when `CO_BOT_SHARED_SECRET` is unset (loopback-only).
- [x] **LOW** — birthday job uses the timed `shared_client()`; `lead_digest` honors `window=` (bot form) + `days=`.
- [x] co-web: `cargo test -p co-web` (1745) + integration suites green; clippy clean. Bot: 136 tests green.
- [x] CHANGELOG-PENDING/CO-518.md written.

## Notes
- Bot fixes live in `tools/whatsapp-bot` (non-git, on disk) — fold into CO-507 (thin
  agent client). The co-web fixes are this PR.
- The lone workspace test failure is the known-flaky co-agent retry test
  ([[feedback_flaky_coagent_retry_test]]), not a regression.
- Root cause addressed structurally: the missing tier was a **bot↔co-web contract test**
  (the e2e pipeline is browser→co-web only). `test_co_contract.py` is that tier.
