---
id: 221
title: "Slim AppStateInner (18 fields) via segregated sub-states"
type: user-story
status: todo
priority: medium
conventional_commit: "refactor(state):"
semver_bump: patch
labels:
  - type:refactor
  - module:state
module: co-web
parent: 227
created_at: 2026-05-18T00:00:00Z
updated_at: 2026-05-19T16:54:37.919331+00:00
---

## As

A maintainer of co-web

## I Need

`AppStateInner`'s 18 unrelated fields segregated into 4 composable sub-states extracted via axum `FromRef`

## So That

Handlers only see the state they actually use, segregated-state and composition principles are honored, and the global `State<AppState>` mega-extractor stops leaking unrelated dependencies into every handler.

## Context

- **Principles:** 5 (segregated state), 1 (composition)
- **Scope:** Split `AppStateInner`'s 18 fields into 4 sub-states: `CoreState` (storage, config, auth), `RealtimeState` (doc_rooms, sync_rooms, chat_rooms, chat_presence), `IndexState` (cache, embeddings, embedding_tx), `IntegrationsState` (mail, geo, plugin_registry, game_storage, wae, jwt_key, rate_limiter, experiment). `AppStateInner` keeps `Arc`s to each. Handlers extract only the sub-state they need via `State<Arc<CoreState>>` (axum supports this via `FromRef`).
- **Why medium:** Best done after CO-215; the value compounds as more handlers migrate.

## Acceptance

- Four new sub-state structs.
- At least 10 handlers updated to take a narrow sub-state.
- `cargo test` passes.
- Existing `State<AppState>` extractor still works for handlers not yet migrated.

## Blast radius

~200 LoC structural change, opt-in per handler thereafter.
