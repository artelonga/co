---
id: 489
title: "WhatsApp tenant routing — forward phone_number_id to the bot brain"
status: done
priority: medium
parent: 20
labels:
  - feat
  - whatsapp
  - multi-tenant
module: co-web (integrations/whatsapp_cloud_routes + bot_proxy_routes)
release: whatsapp-self-sovereign
created_at: 2026-06-26T00:00:00Z
updated_at: 2026-06-26T00:00:00Z
---

GIVEN one bot deploy should serve many customers,
WHEN the inbound Cloud API webhook forwards the business `phone_number_id` (which
number was messaged) to the bot brain,
THEN the brain maps `phone_number_id → universe`, so onboarding a customer is a
config edit, not a code change.

## Acceptance

- [x] `parse_inbound` extracts `metadata.phone_number_id`; `InboundMsg` carries it.
- [x] `fetch_brain_reply` forwards `{text, sender:"whatsapp", phone_number_id}` to
  `CO_BOT_BRAIN_URL`.
- [x] **`scripts/whatsapp-webhook-smoke.sh`** — local Meta→co-web→brain integration
  test (signed payload → fast 200, tenant-id forwarding, forged-sig 401, GET verify
  handshake). 7/7. Fictional numbers only.
- [x] `docs/whatsapp-launch.md` runbook + `CHANGELOG-PENDING/CO-489.md`.
- [x] `cargo clippy -- -D warnings` + tests green.
