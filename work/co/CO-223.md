---
id: 223
title: "Define shared Worker trait + lifecycle (embedding + notification + push workers)"
type: user-story
status: todo
priority: medium
conventional_commit: "refactor(workers):"
semver_bump: patch
labels:
  - type:refactor
  - module:workers
module: co-web
parent: 229
created_at: 2026-05-18T00:00:00Z
updated_at: 2026-05-18T00:00:00Z
---

## As

A maintainer of co-web

## I Need

A shared `Worker` trait and `WorkerSupervisor` that unifies the three current worker patterns (mpsc channel, DB polling, in-process buffer)

## So That

Worker lifecycle (start/stop/health) is composable, panics in one worker do not bring down siblings or poison locks, and ops gets a unified `/api/v1/admin/workers/status` surface.

## Context

- **Principles:** 1 (composition), 7 (event-driven)
- **Scope:** Workers today follow 3 patterns: mpsc channel (embedding_worker), DB polling (notification_*_worker, webhook_worker), in-process buffer (wae). Define `trait Worker { fn name; async fn tick; async fn run_loop; }` and a `WorkerSupervisor` that owns the JoinHandles + shutdown. Each existing worker implements it. Cross-references `feedback_no_panic_under_mutex` — supervisor catches panics and restarts (without poisoning).
- **Why medium:** Operational visibility + safer panic handling.

## Acceptance

- Trait defined.
- 5 workers migrated.
- Supervisor tracks last-tick timestamp, exposes `/api/v1/admin/workers/status`.
- Panic in one worker doesn't bring down siblings.

## Blast radius

~600 LoC trait + supervisor + migrations.
