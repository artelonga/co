//! Universe route tests, split by group (CO-215 `tests/` pattern).

mod access;
mod config;
mod crud;
mod federation;
mod rate_limit_vault;
mod reparent;
mod source;
pub(crate) mod support;
mod themes;
mod token_visibility;
