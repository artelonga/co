openapi: 3.1.0
info:
  title: CO Web API
  description: |
    API da plataforma CO — gestão de conteúdo em grafo.

    Gerado automaticamente a partir de docs/architecture/api-catalog.md.
    Para adicionar ou remover endpoints, atualize o catálogo e rode
    `npm run openapi:gen` em co-web/.

    ## Como adicionar uma rota

    1. Implemente o handler e registre-o no arquivo `*_routes.rs` adequado.
    2. Adicione uma linha na tabela correspondente em `docs/architecture/api-catalog.md`.
    3. Rode `cd co-web && npm run openapi:gen` para regenerar este arquivo.

    ## Envelope opt-in + headers de versão (CO-456 / CO-278-A)

    Toda resposta `/api/v1/*` traz `X-API-Version: 1.0` e
    `X-Co-Server-Version: <versão>`. Envie `X-API-Envelope: 1` (ou
    `Accept: application/vnd.co.v1+json`) para receber o corpo embrulhado no
    schema `Envelope` (`{ data, meta, errors }`). Sem o marcador o corpo é
    idêntico ao formato legado; respostas não-JSON nunca são embrulhadas.
  version: "3.19.0"
  contact:
    name: Arte Longa
    email: yuri@artelonga.com.br
  license:
    name: MIT

servers:
  - url: https://co-artelonga.fly.dev
    description: Production
  - url: http://localhost:8742
    description: Local

tags:
  - name: "auth"
    description: "auth endpoints"
  - name: "universes"
    description: "universes endpoints"
  - name: "git-sync"
    description: "git-sync endpoints"
  - name: "graph-views"
    description: "graph-views endpoints"
  - name: "workspace"
    description: "workspace endpoints"
  - name: "pages"
    description: "pages endpoints"
  - name: "workspace-templates"
    description: "workspace-templates endpoints"
  - name: "calendar"
    description: "calendar endpoints"
  - name: "scrum"
    description: "scrum endpoints"
  - name: "suggest/review"
    description: "suggest/review endpoints"
  - name: "entries"
    description: "entries endpoints"
  - name: "vault"
    description: "vault endpoints"
  - name: "chat"
    description: "chat endpoints"
  - name: "dm"
    description: "dm endpoints"
  - name: "notifications"
    description: "notifications endpoints"
  - name: "interactions"
    description: "interactions endpoints"
  - name: "assets"
    description: "assets endpoints"
  - name: "references"
    description: "references endpoints"
  - name: "admin"
    description: "admin endpoints"
  - name: "gestao"
    description: "gestao endpoints"
  - name: "billing"
    description: "billing endpoints"
  - name: "storage"
    description: "storage endpoints"
  - name: "invitations"
    description: "invitations endpoints"
  - name: "leads"
    description: "leads endpoints"
  - name: "openapi"
    description: "openapi endpoints"
  - name: "agent-sessions"
    description: "agent-sessions endpoints"
  - name: "usage"
    description: "usage endpoints"
  - name: "telemetry"
    description: "telemetry endpoints"
  - name: "ai"
    description: "ai endpoints"
  - name: "chat-llm"
    description: "chat-llm endpoints"
  - name: "changelog"
    description: "changelog endpoints"
  - name: "resolve"
    description: "resolve endpoints"
  - name: "events"
    description: "events endpoints"
  - name: "sync"
    description: "sync endpoints"
  - name: "feedback"
    description: "feedback endpoints"
  - name: "analytics + telemetry"
    description: "analytics + telemetry endpoints"
  - name: "quilombo"
    description: "quilombo endpoints"
  - name: "board"
    description: "board endpoints"
  - name: "experiment"
    description: "experiment endpoints"
  - name: "game"
    description: "game endpoints"
  - name: "live timeline"
    description: "live timeline endpoints"
  - name: "kb"
    description: "kb endpoints"
  - name: "security"
    description: "security endpoints"
  - name: "delivery"
    description: "delivery endpoints"
  - name: "suggest/review pages"
    description: "suggest/review pages endpoints"
  - name: "Health"
    description: "Health endpoints"
  - name: "Metrics"
    description: "Metrics endpoints"
  - name: "crawl"
    description: "crawl endpoints"

paths:
  /.well-known/jwks.json:
    get:
      tags: ["auth"]
      summary: "JWKS (ES256 public key)"
      responses:
        "200":
          description: OK
  /.well-known/openid-configuration:
    get:
      tags: ["auth"]
      summary: "OIDC discovery"
      responses:
        "200":
          description: OK
  /agora:
    get:
      tags: ["live timeline"]
      summary: "CO-381 — live timeline SPA (pt-BR)"
      responses:
        "200":
          description: OK
  /api/docs:
    get:
      tags: ["openapi"]
      summary: "Interactive API explorer"
      responses:
        "200":
          description: OK
  /api/experiment/feedback:
    post:
      tags: ["experiment"]
      summary: "Record feedback"
      responses:
        "200":
          description: OK
  /api/experiment/summary:
    get:
      tags: ["experiment"]
      summary: "Variant summary"
      responses:
        "200":
          description: OK
  /api/experiment/variant:
    get:
      tags: ["experiment"]
      summary: "Get current variant"
      responses:
        "200":
          description: OK
    post:
      tags: ["experiment"]
      summary: "Switch variant"
      responses:
        "200":
          description: OK
  /api/health:
    get:
      tags: ["Health"]
      summary: "Liveness"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/health/deep:
    get:
      tags: ["Health"]
      summary: "Includes storage check"
      responses:
        "200":
          description: OK
  /api/openapi.json:
    get:
      tags: ["openapi"]
      summary: "OpenAPI 3.1 JSON spec"
      responses:
        "200":
          description: OK
  /api/projects:
    post:
      tags: ["board"]
      summary: "Create project"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/projects/{key}:
    get:
      tags: ["board"]
      summary: "Project info"
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["board"]
      summary: "Delete project"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/projects/{key}/activity:
    get:
      tags: ["board"]
      summary: "Activity feed"
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/projects/{key}/dashboard:
    get:
      tags: ["board"]
      summary: "Dashboard"
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/projects/{key}/tasks:
    get:
      tags: ["board"]
      summary: "List tasks"
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["board"]
      summary: "Create task"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTask"
      responses:
        "201":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/projects/{key}/tasks/bulk-delete:
    post:
      tags: ["board"]
      summary: "Bulk delete"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/projects/{key}/tasks/bulk-update:
    post:
      tags: ["board"]
      summary: "Bulk update"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/projects/{key}/tasks/{id}:
    get:
      tags: ["board"]
      summary: "Get single task"
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: ["board"]
      summary: "Update/delete"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["board"]
      summary: "Update/delete"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/projects/{key}/tasks/{id}/comments:
    get:
      tags: ["board"]
      summary: "List comments"
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["board"]
      summary: "Create comment"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/ab/flags:
    get:
      tags: ["admin"]
      summary: "List flags"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["admin"]
      summary: "CO-121 — A/B flag CRUD"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/ab/flags/{key}:
    put:
      tags: ["admin"]
      summary: "Toggle flag"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/admin/backup:
    post:
      tags: ["admin"]
      summary: "CO-459 — reconstructable local snapshot (VACUUM INTO + sha256 manifest); returns verified manifest"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/backup/snapshot:
    post:
      tags: ["admin"]
      summary: "CO-365 — force snapshot via active backup backend"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/backup/snapshots:
    get:
      tags: ["admin"]
      summary: "CO-365 — list snapshots from backup backend"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/changelog/reindex:
    post:
      tags: ["admin"]
      summary: "Re-index changelog from git"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/chat/origin-breakdown:
    get:
      tags: ["admin"]
      summary: "CO-204 — chat message origin-universe breakdown; CO-448 requires capability `chat:read`"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/co-dev:
    get:
      tags: ["admin"]
      summary: "CO-43 dev board info"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/co-dev/entries:
    get:
      tags: ["admin"]
      summary: "Dev board entries"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/co-dev/entries/tags:
    get:
      tags: ["admin"]
      summary: "Dev board tags"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/co-dev/entries/{*path}:
    get:
      tags: ["admin"]
      summary: "Get dev board entry by path"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    put:
      tags: ["admin"]
      summary: "Update dev board entry"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/dashboard:
    get:
      tags: ["admin"]
      summary: "Cross-system dashboard"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/deployments:
    get:
      tags: ["admin"]
      summary: "Deployment history"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/deployments/refresh:
    post:
      tags: ["admin"]
      summary: "Refresh deployment data"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/leads:
    get:
      tags: ["admin"]
      summary: "CO-183 leads queue"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/leads/{id}:
    patch:
      tags: ["admin"]
      summary: "Update lead"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/admin/pipeline/summary:
    get:
      tags: ["admin"]
      summary: "CO-88 content-pipeline per-universe stats"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/storage:
    get:
      tags: ["storage"]
      summary: "Cluster-wide storage breakdown"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/sweep:
    post:
      tags: ["admin"]
      summary: "CO-459 — junk sweep; dry-run default, `?apply=true` to delete"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/telemetry/crud-summary:
    get:
      tags: ["admin"]
      summary: "CRUD events summary"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/telemetry/export:
    get:
      tags: ["admin"]
      summary: "CSV export"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/telemetry/summary:
    get:
      tags: ["admin"]
      summary: "Telemetry summary (CO-46)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/users/origin-breakdown:
    get:
      tags: ["admin"]
      summary: "User signup origin breakdown"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/admin/workers/status:
    get:
      tags: ["admin"]
      summary: "Worker/job status"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/agent/sessions:
    get:
      tags: ["agent-sessions"]
      summary: "List agent sessions (public kanban)"
      responses:
        "200":
          description: OK
    post:
      tags: ["agent-sessions"]
      summary: "Record new agent session"
      security:
        - bearerJWT: []
        - apiToken: []
      responses:
        "200":
          description: OK
  /api/v1/agent/sessions/latest:
    get:
      tags: ["agent-sessions"]
      summary: "Latest session summary"
      responses:
        "200":
          description: OK
  /api/v1/ai/query:
    post:
      tags: ["ai"]
      summary: "AI completion query"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/ai/status:
    get:
      tags: ["ai"]
      summary: "AI provider status"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/analytics/public/funnel:
    get:
      tags: ["analytics + telemetry"]
      summary: "CO-378 — conversion funnel report with private-path redaction"
      responses:
        "200":
          description: OK
  /api/v1/analytics/public/popularity:
    get:
      tags: ["analytics + telemetry"]
      summary: "CO-180 popularity"
      responses:
        "200":
          description: OK
  /api/v1/analytics/public/recent:
    get:
      tags: ["analytics + telemetry"]
      summary: "Recent activity"
      responses:
        "200":
          description: OK
  /api/v1/analytics/public/rollups:
    post:
      tags: ["analytics + telemetry"]
      summary: "Ingest per-universe rollup (CO-340)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/analytics/public/summary:
    get:
      tags: ["analytics + telemetry"]
      summary: "CO-179 public summary"
      responses:
        "200":
          description: OK
  /api/v1/auth/change-password:
    post:
      tags: ["auth"]
      summary: "Change password (authed)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/auth/forgot-password:
    post:
      tags: ["auth"]
      summary: "CO-165 — initiate reset"
      responses:
        "200":
          description: OK
  /api/v1/auth/forgot-password/verify:
    post:
      tags: ["auth"]
      summary: "Verify reset token"
      responses:
        "200":
          description: OK
  /api/v1/auth/github/callback:
    get:
      tags: ["auth"]
      summary: "CO-415 — GitHub OAuth callback"
      responses:
        "200":
          description: OK
  /api/v1/auth/github/start:
    get:
      tags: ["auth"]
      summary: "CO-415 — begin GitHub OAuth (oauth_github.rs)"
      responses:
        "200":
          description: OK
  /api/v1/auth/github/status:
    get:
      tags: ["auth"]
      summary: "Whether GitHub OAuth is configured"
      responses:
        "200":
          description: OK
  /api/v1/auth/google/callback:
    get:
      tags: ["auth"]
      summary: "Google OAuth callback"
      responses:
        "200":
          description: OK
  /api/v1/auth/google/start:
    get:
      tags: ["auth"]
      summary: "Begin Google OAuth (oauth_google.rs)"
      responses:
        "200":
          description: OK
  /api/v1/auth/google/status:
    get:
      tags: ["auth"]
      summary: "Whether Google OAuth is configured"
      responses:
        "200":
          description: OK
  /api/v1/auth/legacy-login:
    post:
      tags: ["game"]
      summary: "Legacy game login"
      responses:
        "200":
          description: OK
  /api/v1/auth/login:
    post:
      tags: ["auth"]
      summary: "Request email magic-code (CO-188)"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginUsuario"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/auth/login-options:
    get:
      tags: ["auth"]
      summary: "CO-303 — login methods available"
      responses:
        "200":
          description: OK
  /api/v1/auth/logout:
    post:
      tags: ["auth"]
      summary: "Clear session cookie"
      responses:
        "200":
          description: OK
  /api/v1/auth/me:
    get:
      tags: ["auth"]
      summary: "Current user + tier"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Usuario"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/auth/onboard-with-email:
    post:
      tags: ["auth"]
      summary: "CO-190 — passwordless onboarding"
      responses:
        "200":
          description: OK
  /api/v1/auth/onboard-with-email/verify:
    post:
      tags: ["auth"]
      summary: "CO-190 — verify code"
      responses:
        "200":
          description: OK
  /api/v1/auth/password-login:
    post:
      tags: ["auth"]
      summary: "CO-85 — username+password (any env)"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginUsuario"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/auth/recovery/channels:
    get:
      tags: ["auth"]
      summary: "List recovery channels"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["auth"]
      summary: "Add recovery channel"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/auth/recovery/channels/verify:
    post:
      tags: ["auth"]
      summary: "Verify channel code"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/auth/recovery/channels/{id}:
    delete:
      tags: ["auth"]
      summary: "Remove recovery channel"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/auth/register:
    post:
      tags: ["game"]
      summary: "Register game player"
      responses:
        "200":
          description: OK
  /api/v1/auth/reset-password:
    post:
      tags: ["auth"]
      summary: "CO-165 — complete reset"
      responses:
        "200":
          description: OK
  /api/v1/auth/signup:
    post:
      tags: ["auth"]
      summary: "CO-175 — usuario + password (+ optional email), rate-limited 100/day"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CriarUsuario"
      responses:
        "201":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/auth/stats:
    get:
      tags: ["auth"]
      summary: "Per-user storage stats"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/auth/token:
    post:
      tags: ["auth"]
      summary: "CO-35 — create long-lived API token; CO-448 accepts optional `scopes` (capability/bundle list) for a least-privilege token"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/auth/tokens:
    get:
      tags: ["auth"]
      summary: "List API tokens"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/auth/tokens/{id}:
    delete:
      tags: ["auth"]
      summary: "Revoke API token"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/auth/uat-login:
    post:
      tags: ["auth"]
      summary: "CO-44 — `yuri/uat` on UAT, 404 in prod"
      responses:
        "200":
          description: OK
  /api/v1/auth/verify:
    post:
      tags: ["auth"]
      summary: "Exchange code → session cookie"
      responses:
        "200":
          description: OK
  /api/v1/billing/webhook/{provider}:
    post:
      tags: ["billing"]
      summary: "Provider webhook — verifies signature, flips tier on success"
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/blobs:
    post:
      tags: ["assets"]
      summary: "CAS write"
      security:
        - bearerJWT: []
        - apiToken: []
      responses:
        "200":
          description: OK
  /api/v1/blobs/{hash}:
    get:
      tags: ["assets"]
      summary: "CAS read (CO-163 BaseBackend shim)"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: hash
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    head:
      tags: ["assets"]
      summary: "CAS exists check"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: hash
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/cache/stats:
    get:
      tags: ["storage"]
      summary: "LRU cache stats (CO-79)"
      responses:
        "200":
          description: OK
  /api/v1/changelog:
    get:
      tags: ["changelog"]
      summary: "Changelog entries"
      responses:
        "200":
          description: OK
  /api/v1/changelog/feed:
    get:
      tags: ["changelog"]
      summary: "Changelog Atom/RSS feed"
      responses:
        "200":
          description: OK
  /api/v1/changelog/repos:
    get:
      tags: ["changelog"]
      summary: "Repositories in changelog"
      responses:
        "200":
          description: OK
  /api/v1/chat/{slug}:
    post:
      tags: ["chat-llm"]
      summary: "LLM chat on a universe"
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/deployments/status:
    get:
      tags: ["chat-llm"]
      summary: "Latest deployment status"
      responses:
        "200":
          description: OK
  /api/v1/dms/with/{user_id}:
    post:
      tags: ["dm"]
      summary: "Open or get DM room"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/dms/{room_id}/mute:
    post:
      tags: ["dm"]
      summary: "Toggle mute"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: room_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/dms/{room_id}/read:
    post:
      tags: ["dm"]
      summary: "Mark read"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: room_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/events:
    get:
      tags: ["events"]
      summary: "WebSocket — subscribe to scoped event stream; filter via `?scope=mine"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/events/bridge:
    get:
      tags: ["events"]
      summary: "CO-384 — federated bridge: remote producers (Yggdrasil) dial in to publish events; CO is the hub"
      responses:
        "200":
          description: OK
  /api/v1/feedback:
    post:
      tags: ["feedback"]
      summary: "Submit feedback (universe-wide)"
      responses:
        "200":
          description: OK
  /api/v1/feedback/all/public:
    get:
      tags: ["feedback"]
      summary: "Public feedback aggregated across universes"
      responses:
        "200":
          description: OK
  /api/v1/feedback/{key}:
    get:
      tags: ["feedback"]
      summary: "List feedback for universe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    patch:
      tags: ["feedback"]
      summary: "Update feedback status"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/feedback/{universe_key}/entry/{*entry_path}:
    get:
      tags: ["feedback"]
      summary: "Feedback for a specific entry"
      parameters:
        - name: universe_key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/feedback/{universe_key}/item/{id}:
    get:
      tags: ["feedback"]
      summary: "Single feedback item"
      parameters:
        - name: universe_key
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/feedback/{universe_key}/public:
    get:
      tags: ["feedback"]
      summary: "Public feedback mural for universe"
      parameters:
        - name: universe_key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/feedback/{universe_key}/{*entry_path}:
    post:
      tags: ["feedback"]
      summary: "Submit feedback for entry"
      parameters:
        - name: universe_key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/games/leaderboard/global:
    get:
      tags: ["game"]
      summary: "Global leaderboard"
      responses:
        "200":
          description: OK
  /api/v1/games/recent:
    get:
      tags: ["game"]
      summary: "Recent game activity"
      responses:
        "200":
          description: OK
  /api/v1/games/{game_name}/leaderboard:
    get:
      tags: ["game"]
      summary: "Game leaderboard"
      parameters:
        - name: game_name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/games/{game_name}/result:
    post:
      tags: ["game"]
      summary: "Record game result"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: game_name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/games/{game_name}/stats:
    get:
      tags: ["game"]
      summary: "My game stats"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: game_name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/gestao/analytics/resumo:
    get:
      tags: ["gestao"]
      summary: "CO-378 — analytics resumo with private-path visibility"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/eventos:
    get:
      tags: ["gestao"]
      summary: "List all eventos"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["gestao"]
      summary: "Create evento"
      security:
        - sessionCookie: []
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CriarEvento"
      responses:
        "201":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Evento"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/gestao/eventos/{id}:
    put:
      tags: ["gestao"]
      summary: "Update evento"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AtualizarEvento"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Evento"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      tags: ["gestao"]
      summary: "Delete evento"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/gestao/manifesto:
    get:
      tags: ["gestao"]
      summary: "Universe Iceberg manifest"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Manifest"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/gestao/manifesto/reconstruir:
    post:
      tags: ["gestao"]
      summary: "Rebuild manifest from source"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/membros:
    get:
      tags: ["gestao"]
      summary: "List all membros"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["gestao"]
      summary: "Create membro"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/membros/{id}:
    put:
      tags: ["gestao"]
      summary: "Update membro"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["gestao"]
      summary: "Delete membro"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/gestao/oauth/clients:
    get:
      tags: ["auth"]
      summary: "List clients"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["auth"]
      summary: "Register OAuth client"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/publicar:
    post:
      tags: ["gestao"]
      summary: "Publish markdown file to universe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublicarRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicarResponse"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/gestao/quadro:
    get:
      tags: ["gestao"]
      summary: "List board items"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["gestao"]
      summary: "Create board item"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/quadro/{id}:
    put:
      tags: ["gestao"]
      summary: "Update board item"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["gestao"]
      summary: "Delete board item"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/gestao/relatos:
    get:
      tags: ["gestao"]
      summary: "List all relatos"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["gestao"]
      summary: "Create relato"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/relatos/{id}:
    put:
      tags: ["gestao"]
      summary: "Update relato"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["gestao"]
      summary: "Delete relato"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/gestao/schema-status:
    get:
      tags: ["gestao"]
      summary: "CO-361 — current schema version + app version"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/security/findings:
    get:
      tags: ["security"]
      summary: "List vulnerability findings"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/security/findings/{id}:
    get:
      tags: ["security"]
      summary: "Finding detail"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    patch:
      tags: ["security"]
      summary: "Resolve finding (patched / accepted-risk / false-positive / wont-fix)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/gestao/security/scan:
    post:
      tags: ["security"]
      summary: "Trigger a scan"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/security/scan/status:
    get:
      tags: ["security"]
      summary: "Last scan status"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/universos/indisponiveis:
    get:
      tags: ["gestao"]
      summary: "CO-406 — list universes the pool marks unavailable"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/universos/{key}/reabrir:
    post:
      tags: ["gestao"]
      summary: "CO-406 — reopen a degraded universe (no restart)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/gestao/users/{id}/mark-paid:
    post:
      tags: ["billing"]
      summary: "Manual provider — mark a user paid"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/gestao/validar:
    post:
      tags: ["gestao"]
      summary: "Validate markdown frontmatter"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidarResponse"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/gestao/webhooks:
    get:
      tags: ["gestao"]
      summary: "List webhooks"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["gestao"]
      summary: "CO-168 register outbound webhook"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/gestao/webhooks/{id}:
    put:
      tags: ["gestao"]
      summary: "Update"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["gestao"]
      summary: "Delete"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/gestao/webhooks/{id}/deliveries:
    get:
      tags: ["gestao"]
      summary: "Delivery log"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/graph-views:
    get:
      tags: ["graph-views"]
      summary: "List public graph views"
      responses:
        "200":
          description: OK
    post:
      tags: ["graph-views"]
      summary: "Create a saved graph view"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/graph-views/{slug}:
    get:
      tags: ["graph-views"]
      summary: "Fetch a saved view"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    patch:
      tags: ["graph-views"]
      summary: "Update visibility/filters"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["graph-views"]
      summary: "Delete"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/health:
    get:
      tags: ["game"]
      summary: "Game service health"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/interactions:
    get:
      tags: ["interactions"]
      summary: "Discover registered interactions"
      responses:
        "200":
          description: OK
  /api/v1/interactions/openapi.json:
    get:
      tags: ["interactions"]
      summary: "**OpenAPI 3.1 generated from `e2e/interactions/registry.yaml`**"
      responses:
        "200":
          description: OK
  /api/v1/interactions/{id}:
    get:
      tags: ["interactions"]
      summary: "Inspect one"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["interactions"]
      summary: "Execution not yet wired"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/invitations/{token}:
    get:
      tags: ["invitations"]
      summary: "Preview invitation before accepting"
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/invitations/{token}/accept:
    post:
      tags: ["invitations"]
      summary: "Accept invite, join universe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/kb/ingest:
    post:
      tags: ["kb"]
      summary: "Ingest entry version into the KB warehouse"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/kb/recent:
    get:
      tags: ["kb"]
      summary: "Latest synced entry versions"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/kb/search:
    get:
      tags: ["kb"]
      summary: "Full-text search over body previews (FTS5)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/leads:
    post:
      tags: ["leads"]
      summary: "Submit interest (public)"
      responses:
        "200":
          description: OK
  /api/v1/me/billing/checkout:
    post:
      tags: ["billing"]
      summary: "Start checkout for `{ plan }`, returns `{ url, session_id, expires_at, provider }`"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/billing/status:
    get:
      tags: ["billing"]
      summary: "Current `{ tier, plan, paid_at, billing_provider }`"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/dm-policy:
    put:
      tags: ["dm"]
      summary: "Who can DM me"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/dms:
    get:
      tags: ["dm"]
      summary: "DM inbox"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/inbound-proposals:
    get:
      tags: ["entries"]
      summary: "Inbox of inbound proposals across owned universes"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/invitations:
    get:
      tags: ["invitations"]
      summary: "My pending invitations"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/invitations/accept:
    post:
      tags: ["invitations"]
      summary: "Accept invite via me/ (token in body)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/notification-preferences:
    get:
      tags: ["notifications"]
      summary: "Get prefs"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    put:
      tags: ["notifications"]
      summary: "Update prefs"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/notifications:
    get:
      tags: ["notifications"]
      summary: "List notifications"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/notifications/read-all:
    post:
      tags: ["notifications"]
      summary: "Mark all read"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/notifications/{id}/read:
    post:
      tags: ["notifications"]
      summary: "Mark single read"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/me/push-subscriptions:
    get:
      tags: ["notifications"]
      summary: "List subscriptions"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["notifications"]
      summary: "Subscribe to push"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/push-subscriptions/{id}:
    delete:
      tags: ["notifications"]
      summary: "Unsubscribe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/me/storage:
    get:
      tags: ["storage"]
      summary: "Per-user storage (CO-128)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/sync/conflicts:
    get:
      tags: ["sync"]
      summary: "List the caller's outstanding sync conflicts (returns `SyncConflict[]`)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/me/universes:
    get:
      tags: ["universes"]
      summary: "Bucketed: owned/member/subscribed (CO-191)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/notifications/vapid-public-key:
    get:
      tags: ["notifications"]
      summary: "CO-201 — VAPID public key"
      responses:
        "200":
          description: OK
  /api/v1/players/{username}:
    get:
      tags: ["game"]
      summary: "Player public profile"
      parameters:
        - name: username
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/plugins:
    get:
      tags: ["game"]
      summary: "List plugins"
      responses:
        "200":
          description: OK
  /api/v1/profile:
    get:
      tags: ["game"]
      summary: "My game profile"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/quilombo/admin/atividades:
    get:
      tags: ["quilombo"]
      summary: "Activity log"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/quilombo/admin/resumo:
    get:
      tags: ["quilombo"]
      summary: "Admin summary"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/quilombo/admin/telemetria:
    get:
      tags: ["quilombo"]
      summary: "Telemetria quilombo"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/quilombo/admin/usuarios:
    get:
      tags: ["quilombo"]
      summary: "List users"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/quilombo/admin/usuarios/{id}:
    put:
      tags: ["quilombo"]
      summary: "Update user"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/alterar-pagina-na-web/approve/{run_id}:
    post:
      tags: ["quilombo"]
      summary: "Approve run"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: run_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/alterar-pagina-na-web/preview:
    post:
      tags: ["quilombo"]
      summary: "Preview change"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/quilombo/alterar-pagina-na-web/revert:
    post:
      tags: ["quilombo"]
      summary: "Revert run"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/quilombo/alterar-pagina-na-web/runs:
    get:
      tags: ["quilombo"]
      summary: "List web-edit runs"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/quilombo/auth/cadastro:
    post:
      tags: ["quilombo"]
      summary: "Quilombo registration"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CriarUsuario"
      responses:
        "201":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/quilombo/auth/link-co-account:
    post:
      tags: ["quilombo"]
      summary: "Link to CO account"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/quilombo/auth/login:
    post:
      tags: ["quilombo"]
      summary: "Quilombo login (user+password)"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginUsuario"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/quilombo/comentarios:
    get:
      tags: ["quilombo"]
      summary: "List comments"
      responses:
        "200":
          description: OK
    post:
      tags: ["quilombo"]
      summary: "Post comment (anonymous allowed)"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CriarComentario"
      responses:
        "201":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Comentario"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/quilombo/contato:
    post:
      tags: ["quilombo"]
      summary: "Contact form"
      responses:
        "200":
          description: OK
  /api/v1/quilombo/eventos:
    get:
      tags: ["quilombo"]
      summary: "List eventos"
      responses:
        "200":
          description: OK
  /api/v1/quilombo/eventos/criar:
    post:
      tags: ["quilombo"]
      summary: "Create evento (admin)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CriarEvento"
      responses:
        "201":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Evento"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/quilombo/eventos/slug/{slug}:
    get:
      tags: ["quilombo"]
      summary: "Get evento by slug"
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/eventos/{id}:
    get:
      tags: ["quilombo"]
      summary: "Get evento by ID"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/eventos/{id}/editar:
    put:
      tags: ["quilombo"]
      summary: "Update evento (admin)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AtualizarEvento"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Evento"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/quilombo/eventos/{id}/excluir:
    post:
      tags: ["quilombo"]
      summary: "Delete evento (admin)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/fotos/{filename}:
    get:
      tags: ["quilombo"]
      summary: "Serve photo file"
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/membros:
    get:
      tags: ["quilombo"]
      summary: "List membros"
      responses:
        "200":
          description: OK
  /api/v1/quilombo/membros/{usuario}:
    get:
      tags: ["quilombo"]
      summary: "Member public profile"
      parameters:
        - name: usuario
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/mensagens:
    get:
      tags: ["quilombo"]
      summary: "My messages"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["quilombo"]
      summary: "Send message"
      security:
        - sessionCookie: []
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CriarMensagem"
      responses:
        "201":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Mensagem"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/quilombo/missoes:
    get:
      tags: ["quilombo"]
      summary: "List missoes"
      responses:
        "200":
          description: OK
  /api/v1/quilombo/missoes/criar:
    post:
      tags: ["quilombo"]
      summary: "Create missao (admin)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CriarMissao"
      responses:
        "201":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Missao"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/quilombo/missoes/{id}:
    get:
      tags: ["quilombo"]
      summary: "Get missao with participants"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/missoes/{id}/participacoes/{uid}:
    put:
      tags: ["quilombo"]
      summary: "Approve/reject participation"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: uid
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AtualizarParticipacao"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Participacao"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/quilombo/missoes/{id}/participar:
    post:
      tags: ["quilombo"]
      summary: "Join missao"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/paginas/{slug}:
    get:
      tags: ["quilombo"]
      summary: "Get static page"
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/perfil:
    get:
      tags: ["quilombo"]
      summary: "My quilombo profile"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Usuario"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: ["quilombo"]
      summary: "Update profile"
      security:
        - sessionCookie: []
        - bearerJWT: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AtualizarPerfil"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Usuario"
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/quilombo/publicacoes:
    get:
      tags: ["quilombo"]
      summary: "List published relatos"
      responses:
        "200":
          description: OK
  /api/v1/quilombo/publicacoes/{slug}:
    get:
      tags: ["quilombo"]
      summary: "Get relato by slug"
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/tags:
    get:
      tags: ["quilombo"]
      summary: "All tags"
      responses:
        "200":
          description: OK
  /api/v1/quilombo/tags/{tag}:
    get:
      tags: ["quilombo"]
      summary: "Relatos by tag"
      parameters:
        - name: tag
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/quilombo/upload/{filename}:
    get:
      tags: ["quilombo"]
      summary: "Serve upload file"
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/resolve:
    get:
      tags: ["resolve"]
      summary: "Resolve a surface ref (query `?ref=<key>::<path>`) → `{ url, universe, deployable_ancestor }`; unknown key → 404, ambiguous/malformed → 400"
      responses:
        "200":
          description: OK
  /api/v1/sala/scope:
    get:
      tags: ["workspace"]
      summary: "CO-399: resolve a scope (`?u=a,b`, or all if absent) → visibility-filtered universes"
      responses:
        "200":
          description: OK
  /api/v1/sala/state:
    get:
      tags: ["workspace"]
      summary: "CO-399: caller's saved state for a multi-universe scope (`?scope=a,b`)"
      responses:
        "200":
          description: OK
    put:
      tags: ["workspace"]
      summary: "CO-399: persist state for a multi-universe scope (`?scope=a,b`)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/sala/state/share:
    post:
      tags: ["workspace"]
      summary: "CO-399: publish a multi-universe scope state as shareable"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/scrum/calendar:
    get:
      tags: ["scrum"]
      summary: "Sprint calendar JSON (past + future sprints, DoD %, events). Query params: `past=6` (default), `future=4` (default)."
      responses:
        "200":
          description: OK
  /api/v1/scrum/calendar.ics:
    get:
      tags: ["scrum"]
      summary: "iCalendar export for Google/Apple Calendar subscription. Returns `text/calendar`."
      responses:
        "200":
          description: OK
  /api/v1/search:
    get:
      tags: ["entries"]
      summary: "CO-164 cross-universe semantic search"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/sync/conflicts/{id}/resolve:
    post:
      tags: ["sync"]
      summary: "Resolve a conflict; body `{action, apply_to_all_matching?}` — actions: `keep_both`, `ignore`, `replace`, `update`, `upsert`, `accept_delete`, `keep_local`"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/telemetry/agent/sessions:
    get:
      tags: ["telemetry"]
      summary: "List agent sessions; filters `?model=&since=`, pagination `?limit=&offset=`"
      security:
        - bearerJWT: []
        - apiToken: []
      responses:
        "200":
          description: OK
  /api/v1/telemetry/agent/sessions/{id}:
    get:
      tags: ["telemetry"]
      summary: "One agent session by id (404 if absent)"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/telemetry/deployments:
    get:
      tags: ["telemetry"]
      summary: "Latest deployment snapshot per app/unit; `?limit=` caps units"
      security:
        - bearerJWT: []
        - apiToken: []
      responses:
        "200":
          description: OK
  /api/v1/telemetry/event:
    post:
      tags: ["analytics + telemetry"]
      summary: "Single event"
      responses:
        "200":
          description: OK
  /api/v1/telemetry/events:
    post:
      tags: ["analytics + telemetry"]
      summary: "Batch (Plausible-style)"
      responses:
        "200":
          description: OK
  /api/v1/telemetry/metrics/release-cadence:
    get:
      tags: ["telemetry"]
      summary: "Releases per period from `release_notes` (`?since=` ISO date, `?limit=`)"
      security:
        - bearerJWT: []
        - apiToken: []
      responses:
        "200":
          description: OK
  /api/v1/telemetry/metrics/throughput:
    get:
      tags: ["telemetry"]
      summary: "Token/session throughput over a window (`?since=`), with per-model breakdown"
      security:
        - bearerJWT: []
        - apiToken: []
      responses:
        "200":
          description: OK
  /api/v1/telemetry/metrics/token-budget:
    get:
      tags: ["telemetry"]
      summary: "Spend vs `CO_AUTO_SOFT_LIMIT_5H_TOKENS` budget over a window (`?since=`)"
      security:
        - bearerJWT: []
        - apiToken: []
      responses:
        "200":
          description: OK
  /api/v1/themes/available:
    get:
      tags: ["universes"]
      summary: "List available themes per tier"
      responses:
        "200":
          description: OK
  /api/v1/themes/{preset}:
    get:
      tags: ["universes"]
      summary: "Compiled CSS for a specific theme preset"
      parameters:
        - name: preset
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/uat/changes:
    get:
      tags: ["admin"]
      summary: "CO-45 UAT change list"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/uat/export-patch:
    post:
      tags: ["admin"]
      summary: "Export changes as patch"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/universes:
    get:
      tags: ["universes"]
      summary: "List universes (public + caller's owned/member)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["universes"]
      summary: "Create universe (CO-444: API-token or session; body may set `visibility` ∈ private/public/unlisted and `parent_key`)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/universes/apply-template-all:
    post:
      tags: ["universes"]
      summary: "Apply template across all universes"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/universes/public:
    get:
      tags: ["universes"]
      summary: "Public universe directory"
      responses:
        "200":
          description: OK
  /api/v1/universes/quilomboaraucaria/stats:
    get:
      tags: ["universes"]
      summary: "Special-cased stats (CO-41)"
      responses:
        "200":
          description: OK
  /api/v1/universes/search:
    get:
      tags: ["universes"]
      summary: "Search public universes"
      responses:
        "200":
          description: OK
  /api/v1/universes/trash:
    get:
      tags: ["universes"]
      summary: "List caller's trashed + archived universes (CO-96)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/universes/{key}/members:
    post:
      tags: ["universes"]
      summary: "Add member"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{key}/members/{user_id}:
    delete:
      tags: ["universes"]
      summary: "Remove member"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: user_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{key}/scrum/backlog:
    get:
      tags: ["scrum"]
      summary: "`{ pbis[], total }` — `pbi` entries. Query `status=` (backlog/ready/in-sprint/done) and `sprint=` filter."
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{key}/scrum/manifest:
    get:
      tags: ["scrum"]
      summary: "Parsed `_scrum.yaml` (`enabled`, `sprint_length_days`, `sprint_start_anchor`, `release_tag_pattern`, `roles`, `default_dod`, …) plus the computed `current_sprint` (or `null` when disabled / no anchor)."
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{key}/scrum/pbi:
    post:
      tags: ["scrum"]
      summary: "Create a PBI (sugar over an entry write at `scrum/pbi/{id}.md`). Body: `{ id, title, priority, points?, status?, acceptance?[], sprint?, assignees?[], epic?, body? }`. `422` on invalid frontmatter. Returns the created entry."
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{key}/scrum/pbi/{id}/dod:
    patch:
      tags: ["scrum"]
      summary: "Check off a Definition-of-Done item. Body: `{ index, done }`. Seeds the checklist from `_scrum.yaml.default_dod` on first toggle. Returns `{ path, dod[] }`. `404` if the PBI is absent."
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{key}/scrum/sprints:
    get:
      tags: ["scrum"]
      summary: "`{ sprints[], total }` — all `sprint` entries, sorted by `number`. Each: `{ path, type, title, frontmatter, body }`."
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{key}/scrum/sprints/current:
    get:
      tags: ["scrum"]
      summary: "Current sprint computed from cadence + anchor: `{ number, start_at, end_at, release_window }`. `404` when Scrum is disabled or `sprint_start_anchor` is missing."
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{key}/workspaces/{slug}/state:
    get:
      tags: ["workspace"]
      summary: "Get current user's workspace state for this sala"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    put:
      tags: ["workspace"]
      summary: "Persist workspace state (positions, custom edges)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{key}/workspaces/{slug}/state/public:
    get:
      tags: ["workspace"]
      summary: "Get public/shared workspace state"
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{key}/workspaces/{slug}/state/share:
    post:
      tags: ["workspace"]
      summary: "Publish workspace state as shareable"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}:
    get:
      tags: ["universes"]
      summary: "Universe info"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    put:
      tags: ["universes"]
      summary: "Update name/visibility/parent_key (CO-423; CO-444 visibility ∈ private/public/unlisted, API-token accepted)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["universes"]
      summary: "Soft-delete universe — sets `deleted_at`, recoverable via trash (CO-96)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/apply-template:
    post:
      tags: ["universes"]
      summary: "Apply template universe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/archive:
    post:
      tags: ["universes"]
      summary: "Archive (soft-hide) universe (CO-96)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/assets:
    get:
      tags: ["assets"]
      summary: "List assets in universe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["assets"]
      summary: "Upload asset (drag-and-drop)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/assets/tags:
    get:
      tags: ["assets"]
      summary: "Tag counts"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/assets/{sha256}:
    get:
      tags: ["assets"]
      summary: "Asset metadata"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: sha256
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["assets"]
      summary: "Delete"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: sha256
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/assets/{sha256}/tags:
    post:
      tags: ["assets"]
      summary: "Add tags"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: sha256
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/assets/{sha256}/tags/{tag}:
    delete:
      tags: ["assets"]
      summary: "Remove tag"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: sha256
          in: path
          required: true
          schema:
            type: string
        - name: tag
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/blob/{*path}:
    get:
      tags: ["assets"]
      summary: "Serve raw blob (PDF viewer fallback)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/branches:
    post:
      tags: ["entries"]
      summary: "Phase 2 — named pointer to state"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/branches/{name}:
    put:
      tags: ["entries"]
      summary: "Advance branch"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/calendar:
    get:
      tags: ["calendar"]
      summary: "Calendar lens config: `{ default_lens, lenses[] }`. Each lens: `id`, `name`, `scale` (linear or log), `epoch_ms?`, `canonical_field?` (default `event_at_ms`), `canonical_type?` (i64_ms, f64_years or i64_units), `week_length_days?`, `weekday_names?[]`, `month_length?`, `timezone?`, `display_format?`, `display_unit?`, `epoch?`, `custom_event_field?`, `cell_duration_ms?`, `break_duration_ms?`, `label_periods?[{name,at}]`. Falls back to the built-in Gregorian lens when the universe has no `_calendar.yaml`."
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/changelog:
    get:
      tags: ["entries"]
      summary: "CO-75 — auto-generated Keep-a-Changelog from the op log (`?since=&until=` RFC3339)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/chat/rooms:
    get:
      tags: ["chat"]
      summary: "List rooms in universe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["chat"]
      summary: "Create room (member-only)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/chat/rooms/{room_slug}/members:
    get:
      tags: ["chat"]
      summary: "Room member list"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: room_slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/chat/rooms/{room_slug}/messages:
    get:
      tags: ["chat"]
      summary: "Paginated history"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: room_slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["chat"]
      summary: "Post message"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: room_slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/chat/rooms/{room_slug}/messages/{msg_id}:
    patch:
      tags: ["chat"]
      summary: "Edit own message"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: room_slug
          in: path
          required: true
          schema:
            type: string
        - name: msg_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["chat"]
      summary: "Delete own message"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: room_slug
          in: path
          required: true
          schema:
            type: string
        - name: msg_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/cherry-pick:
    post:
      tags: ["entries"]
      summary: "Cherry-pick operation"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/citations:
    get:
      tags: ["entries"]
      summary: "Inbound references"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/citations/orphan-wikilinks:
    get:
      tags: ["entries"]
      summary: "Wikilinks with no target entry"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/claim:
    post:
      tags: ["universes"]
      summary: "Claim anon clone after login"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/clone:
    post:
      tags: ["universes"]
      summary: "Auto-clone template (anonymous flow)"
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/comments:
    get:
      tags: ["entries"]
      summary: "Threaded comments for an entry — a VIEW over its anchored thread (CO-472)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["entries"]
      summary: "Add a comment to an entry's anchored thread; auto-creates it; `parent_id` ⇒ recursive reply (CO-472)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/commit-timeline:
    get:
      tags: ["git-sync"]
      summary: "Mermaid timeline of recent commits, grouped by day (CO-89)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/config:
    get:
      tags: ["universes"]
      summary: "Universe config"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    put:
      tags: ["universes"]
      summary: "Update config"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/connections:
    get:
      tags: ["entries"]
      summary: "Typed links / conexões for an entry — outbound + inbound (CO-471, wraps CO-74)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/delivery/github:
    post:
      tags: ["delivery"]
      summary: "GitHub webhook — branch/PR/merge events → status transitions"
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/delivery/metrics:
    get:
      tags: ["delivery"]
      summary: "Lead-time metrics per column (task_status_log)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/dev-tasks:
    get:
      tags: ["entries"]
      summary: "CO-272 dev board tasks for dogfooding"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/diff:
    get:
      tags: ["entries"]
      summary: "Diff between op-log positions"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/duplicate:
    post:
      tags: ["universes"]
      summary: "Duplicate as new universe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/entries:
    get:
      tags: ["entries"]
      summary: "List entries (filter by type)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["entries"]
      summary: "Create entry"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/entries/blocks:
    get:
      tags: ["entries"]
      summary: "Entry content parsed into the block tree (CO-470)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    patch:
      tags: ["entries"]
      summary: "Apply id-addressed block ops (replace/insert_after/delete/move) and re-write the body (CO-473)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/entries/diff:
    get:
      tags: ["entries"]
      summary: "CO-75 — op-level diff of an entry between two instants (`?path=&from=&to=` RFC3339)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/entries/history:
    get:
      tags: ["entries"]
      summary: "Entry change history"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/entries/popular:
    get:
      tags: ["entries"]
      summary: "Popular entries by view count"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/entries/similar:
    get:
      tags: ["entries"]
      summary: "CO-164 — semantic similar"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/entries/tags:
    get:
      tags: ["entries"]
      summary: "Tag counts"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/entries/tree:
    get:
      tags: ["entries"]
      summary: "Hierarchical tree"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/entries/versions:
    get:
      tags: ["entries"]
      summary: "CO-54 — pre-overwrite version snapshots (audit trail)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/entries/{*path}:
    get:
      tags: ["entries"]
      summary: "Get entry by path (CO-75: `?as_of=<RFC3339>` reconstructs a past state)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    put:
      tags: ["entries"]
      summary: "Update entry"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["entries"]
      summary: "Delete entry"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/gantt:
    get:
      tags: ["git-sync"]
      summary: "Mermaid gantt of in-flight tasks, swimlane by assignee (CO-89)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/git-source:
    post:
      tags: ["git-sync"]
      summary: "Set/clear `git_source` + `git_branch` (CO-89)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/git-sync:
    post:
      tags: ["git-sync"]
      summary: "Ingest `git log` now → commit/profile entries + analytics (CO-89)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/graph:
    get:
      tags: ["entries"]
      summary: "CO-335 — entry relation graph (nodes + edges)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/invitations:
    get:
      tags: ["universes"]
      summary: "List universe invitations"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["universes"]
      summary: "Create invitation (CO-188)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/invites:
    get:
      tags: ["universes"]
      summary: "List pending invites (CO-444)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["universes"]
      summary: "Create invite — `{email|handle, role}` (CO-444; API-token accepted)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/invites/{token}:
    delete:
      tags: ["universes"]
      summary: "Revoke an invite by token_hash (CO-444)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/invites/{token}/accept:
    post:
      tags: ["universes"]
      summary: "Accept an invite (universe-scoped, CO-444)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/jobs/doc-gen:
    post:
      tags: ["universes"]
      summary: "Submit doc-generation job"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/jobs/doc-gen/last-error:
    get:
      tags: ["universes"]
      summary: "Last doc-gen job error"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/manifest:
    get:
      tags: ["entries"]
      summary: "Universe manifest (all entries)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/members:
    get:
      tags: ["universes"]
      summary: "List members"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/merges:
    post:
      tags: ["entries"]
      summary: "Merge proposal"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/messages:
    post:
      tags: ["universes"]
      summary: "Contact form — message the universe owner; body `{from_email,from_name,subject,body,website?}` (`website` = honeypot); rate-limited 5/hr/IP; delivers via email + in-app notification (CO-326)"
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/op-diff:
    get:
      tags: ["entries"]
      summary: "Per-op diff view"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/oplog:
    get:
      tags: ["entries"]
      summary: "Full op-log with payloads"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/ops:
    get:
      tags: ["entries"]
      summary: "List operations in op-log"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/projects:
    get:
      tags: ["universes"]
      summary: "List projects in universe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/promote:
    post:
      tags: ["entries"]
      summary: "Promote branch tip"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/proposals:
    post:
      tags: ["entries"]
      summary: "Phase 3 — state-snapshot proposal"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/proposals/decide:
    post:
      tags: ["entries"]
      summary: "Merge/reject inline proposal"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/proposals/inline:
    post:
      tags: ["entries"]
      summary: "Lightweight inline proposal (non-owner)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/publish:
    post:
      tags: ["suggest/review"]
      summary: "CO-418: render-review-publish with traceback — conventional commit + semver intent, stamps `requested_by`, preserves `source`; idempotent re-publish; surfaces `origin`/`requested_by` typed relations"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/query:
    get:
      tags: ["entries"]
      summary: "Query DSL (CO-74) — deprecated alias of `search` (CO-471)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["entries"]
      summary: "CO-244 SQL query (read-only, auth required)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/recompute-analytics:
    post:
      tags: ["git-sync"]
      summary: "Recompute every profile's analytics struct (CO-89)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/reindex:
    post:
      tags: ["universes"]
      summary: "Rebuild search index"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/relations/inbound:
    get:
      tags: ["entries"]
      summary: "CO-153 inbound typed FKs"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/relations/outbound:
    get:
      tags: ["entries"]
      summary: "CO-153 outbound typed FKs"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/replay:
    get:
      tags: ["entries"]
      summary: "Replay op-log to a point"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/restore:
    post:
      tags: ["universes"]
      summary: "Restore a trashed/archived universe (CO-96)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/revert:
    post:
      tags: ["entries"]
      summary: "Revert to prior state"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/review:
    get:
      tags: ["suggest/review"]
      summary: "List draft + reviewed entries for the review queue"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    patch:
      tags: ["suggest/review"]
      summary: "Edit a draft before approving"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/review/approve:
    post:
      tags: ["suggest/review"]
      summary: "Approve → review_status published"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/review/reject:
    post:
      tags: ["suggest/review"]
      summary: "Reject → entry removed"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/search:
    get:
      tags: ["entries"]
      summary: "Search — canonical name for the query DSL interface (CO-471)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/source:
    patch:
      tags: ["universes"]
      summary: "Patch universe source config"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/states:
    post:
      tags: ["entries"]
      summary: "CO-native versioning Phase 1 — snapshot"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/states/diff:
    get:
      tags: ["entries"]
      summary: "Diff between two states"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/storage:
    get:
      tags: ["storage"]
      summary: "Per-universe breakdown"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/subscribe:
    post:
      tags: ["universes"]
      summary: "Subscribe to universe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["universes"]
      summary: "Unsubscribe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/subscribe/pin:
    put:
      tags: ["universes"]
      summary: "Pin subscription"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["universes"]
      summary: "Unpin subscription"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/subscribers:
    get:
      tags: ["universes"]
      summary: "List subscribers"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/subscription:
    get:
      tags: ["universes"]
      summary: "My subscription state"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/suggest:
    post:
      tags: ["suggest/review"]
      summary: "Submit a suggestion (anon-friendly, rate-limited) — creates a draft entry"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/theme.css:
    get:
      tags: ["universes"]
      summary: "Compiled theme CSS"
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/timeline:
    get:
      tags: ["entries"]
      summary: "CO-396 project-timeline lens. Query `group_by=epic|module|status` (default `status`). Returns `{ group_by, lanes[{key,label,items[{id,title,entry_type,span{start_ms,end_ms}}]}], markers[{id,title,kind,at_ms}], backlog[{id,title,entry_type}] }` — the same entries as the kanban board laid out by the shared `game_core::time_layout` engine (lanes + release/milestone markers + a no-date backlog)."
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/translate/{*path}:
    post:
      tags: ["entries"]
      summary: "CO-416 — generate pt↔en content twin (`?to=en`); 503 if backend unconfigured"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/translation/{*path}:
    get:
      tags: ["entries"]
      summary: "CO-416 — does a translation twin exist (+ stale flag)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/vault/:
    get:
      tags: ["vault"]
      summary: "List notes/files"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/vault/clip:
    post:
      tags: ["vault"]
      summary: "Web clipper ingest"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/vault/search:
    post:
      tags: ["vault"]
      summary: "Full-text search"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/vault/tags:
    get:
      tags: ["vault"]
      summary: "Tag counts"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/vault/tree:
    get:
      tags: ["vault"]
      summary: "File tree"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/vault/{*path}:
    get:
      tags: ["vault"]
      summary: "File CRUD (Obsidian REST shape; PATCH for partial updates)"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["vault"]
      summary: "Create file at path"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    put:
      tags: ["vault"]
      summary: "File CRUD (Obsidian REST shape; PATCH for partial updates)"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    patch:
      tags: ["vault"]
      summary: "File CRUD (Obsidian REST shape; PATCH for partial updates)"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["vault"]
      summary: "File CRUD (Obsidian REST shape; PATCH for partial updates)"
      security:
        - bearerJWT: []
        - apiToken: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/workspace-templates:
    get:
      tags: ["workspace-templates"]
      summary: "List available workspace templates"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/workspace-templates/{template_slug}:
    get:
      tags: ["workspace-templates"]
      summary: "Fetch a specific template"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: template_slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{slug}/workspaces/{workspace_slug}/from-template/{template_slug}:
    post:
      tags: ["workspace-templates"]
      summary: "Seed a new workspace from a template"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
        - name: workspace_slug
          in: path
          required: true
          schema:
            type: string
        - name: template_slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{u}/references:
    get:
      tags: ["references"]
      summary: "List reference cards"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: u
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    post:
      tags: ["references"]
      summary: "Create reference card"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: u
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{u}/references/broken-cards:
    get:
      tags: ["references"]
      summary: "Cards with missing blob/file"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: u
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{u}/references/orphan-blobs:
    get:
      tags: ["references"]
      summary: "Blobs with no reference card"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: u
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{u}/references/works:
    get:
      tags: ["references"]
      summary: "List works"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: u
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/universes/{u}/references/{*path}:
    get:
      tags: ["references"]
      summary: "Get reference card"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: u
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    put:
      tags: ["references"]
      summary: "Update reference card"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: u
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["references"]
      summary: "Delete reference card"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: u
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/usage/active:
    get:
      tags: ["usage"]
      summary: "Launchers active right now"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/usage/heartbeat:
    post:
      tags: ["usage"]
      summary: "Register/refresh an active launcher (in-memory TTL)"
      security:
        - bearerJWT: []
        - apiToken: []
      responses:
        "200":
          description: OK
  /api/v1/usage/projects:
    get:
      tags: ["usage"]
      summary: "Fleet roll-up: usage + board + last deploy per universe"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/usage/sessions:
    get:
      tags: ["usage"]
      summary: "Recent per-session listing with tool usage + PR link (CO-437)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
    post:
      tags: ["usage"]
      summary: "Ingest a session's token/cost + metadata report (CO-425/CO-437 producer)"
      security:
        - bearerJWT: []
        - apiToken: []
      responses:
        "200":
          description: OK
  /api/v1/usage/summary:
    get:
      tags: ["usage"]
      summary: "Aggregates by universe|model|machine|task; `?cross=universe,model` adds the cross-tab matrix (CO-437)"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/users/{user_id}/block:
    post:
      tags: ["dm"]
      summary: "Block user"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
    delete:
      tags: ["dm"]
      summary: "Unblock"
      security:
        - sessionCookie: []
        - bearerJWT: []
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /api/v1/wallet:
    get:
      tags: ["game"]
      summary: "My wallet"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /api/v1/workspace-states/{token}:
    get:
      tags: ["workspace"]
      summary: "Fetch a shared workspace state by token (carries `scope` for multi-universe shares)"
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /auth/co-handover:
    get:
      tags: ["auth"]
      summary: "CO-206 — issue handover token, server-side redirect"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /changelog/embed:
    get:
      tags: ["changelog"]
      summary: "Iframeable releases-as-sprints view (CSP frame-ancestors *.artelonga.com.br)"
      responses:
        "200":
          description: OK
  /co/{slug}/{*path}:
    get:
      tags: ["pages"]
      summary: "Universe SPA shell — deep-links into a universe entry/board path"
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /gestao:
    get:
      tags: ["gestao"]
      summary: "CO-361 — gestao SPA shell page"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /live:
    get:
      tags: ["live timeline"]
      summary: "CO-381 — live timeline SPA (en) — same SPA as /agora"
      responses:
        "200":
          description: OK
  /metrics:
    get:
      tags: ["Metrics"]
      summary: "Queue depth, dead-letter count, autoscale recommendation, per-kind success rate + p99 latency"
      responses:
        "200":
          description: OK
  /oauth/authorize:
    get:
      tags: ["auth"]
      summary: "OAuth2 authorization code flow"
      security:
        - sessionCookie: []
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /oauth/token:
    post:
      tags: ["auth"]
      summary: "Token exchange"
      responses:
        "200":
          description: OK
  /oauth/userinfo:
    get:
      tags: ["auth"]
      summary: "OIDC userinfo"
      security:
        - bearerJWT: []
        - apiToken: []
      responses:
        "200":
          description: OK
  /robots.txt:
    get:
      tags: ["crawl"]
      summary: "Crawl policy (private paths disallowed)"
      responses:
        "200":
          description: OK
  /sala:
    get:
      tags: ["workspace"]
      summary: "CO-399: Sala SPA shell — every caller-visible universe (`/sala?u=a,b` for a subset)"
      responses:
        "200":
          description: OK
  /scrum/calendar:
    get:
      tags: ["scrum"]
      summary: "Sprint calendar SPA page (standalone HTML)."
      responses:
        "200":
          description: OK
  /sitemap.xml:
    get:
      tags: ["crawl"]
      summary: "Public universes/entries only"
      responses:
        "200":
          description: OK
  /u/{universe}/sala:
    get:
      tags: ["workspace"]
      summary: "Sala SPA shell (default workspace for universe)"
      parameters:
        - name: universe
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /u/{universe}/sala/{workspace_slug}:
    get:
      tags: ["workspace"]
      summary: "Sala SPA shell (specific workspace)"
      parameters:
        - name: universe
          in: path
          required: true
          schema:
            type: string
        - name: workspace_slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /v1/log-drains/vercel/{universe_id}:
    post:
      tags: ["admin"]
      summary: "CO-124 Vercel log drain receiver"
      security:
        - sharedSecret: []
      parameters:
        - name: universe_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /v1/metrics:
    post:
      tags: ["usage"]
      summary: "OTLP/HTTP metrics receiver — ingests Claude Code native `claude_code.*` telemetry into `usage_sessions` (CO-457)"
      security:
        - bearerJWT: []
      responses:
        "200":
          description: OK
  /{slug}/review:
    get:
      tags: ["suggest/review pages"]
      summary: "Owner review queue page (shared/review.html)"
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
  /{slug}/suggest:
    get:
      tags: ["suggest/review pages"]
      summary: "Public suggest form page (shared/suggest.html)"
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK

components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: session
      description: JWT session cookie (set via /api/v1/auth/verify or password-login)
    apiToken:
      type: http
      scheme: bearer
      description: Long-lived API token (created via POST /api/v1/auth/token)
    bearerJWT:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT token via Authorization header (legacy; prefer sessionCookie)
    githubPAT:
      type: http
      scheme: bearer
      description: GitHub Personal Access Token (gestão administrativa)
    sharedSecret:
      type: apiKey
      in: header
      name: X-Shared-Secret
      description: Shared secret for server-to-server endpoints (e.g. log drains)

  schemas:
    # --- Erros ---
    Error:
      type: object
      required: [error, message]
      properties:
        error:
          type: string
          example: not_found
        message:
          type: string
          example: "Recurso não encontrado"

    # --- Envelope opt-in (CO-456 / CO-278-A) ---
    # Activated by `X-API-Envelope: 1` (or Accept: application/vnd.co.v1+json).
    # Without the marker the body is the legacy shape (byte-identical).
    EnvelopeMeta:
      type: object
      required: [request_id, api_version]
      properties:
        request_id:
          type: string
          format: uuid
          description: Per-request id (mirrors the x-request-id header)
        api_version:
          type: string
          example: "1.0"
        page:
          type: integer
          description: Present only when the handler exposes pagination
        page_size:
          type: integer
        total:
          type: integer
    EnvelopeError:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          example: not_found
        message:
          type: string
          example: "Resource not found"
        field:
          type: string
          description: Offending input field (when applicable)
        hint:
          type: string
          description: How to fix the error (when applicable)
    Envelope:
      type: object
      required: [data, meta, errors]
      description: >
        Opt-in unified response shape for /api/v1/*. On success `data` holds the
        original body and `errors` is null; on error (4xx/5xx) `data` is null and
        `errors` lists the problems. Same HTTP status either way.
      properties:
        data:
          nullable: true
          description: Original handler body on success; null on error
        meta:
          $ref: '#/components/schemas/EnvelopeMeta'
        errors:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/EnvelopeError'

    # --- Projetos / Tarefas ---
    Task:
      type: object
      required: [id, key, project_key, title, status, priority, labels, created_at, updated_at, description, archived]
      properties:
        id:
          type: integer
          example: 1
        key:
          type: string
          example: DS-1
        project_key:
          type: string
          example: DS
        title:
          type: string
          example: Define visual identity
        status:
          type: string
          enum: [todo, in_progress, in_review, done]
        priority:
          type: string
          enum: [low, medium, high, critical]
        due_date:
          type: string
          format: date
          nullable: true
          example: "2026-04-30"
        parent:
          type: integer
          nullable: true
        labels:
          type: array
          items:
            type: string
        description:
          type: string
        archived:
          type: boolean
          default: false
        assignee:
          type: string
          nullable: true
          description: Name or email of the person assigned to this task
          example: "yuri@artelonga.com.br"
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CreateTask:
      type: object
      required: [title]
      properties:
        title:
          type: string
        description:
          type: string
          default: ""
        status:
          type: string
          enum: [todo, in_progress, in_review, done]
          default: todo
        priority:
          type: string
          enum: [low, medium, high, critical]
          default: medium
        due_date:
          type: string
          format: date
          nullable: true
        parent:
          type: integer
          nullable: true
        labels:
          type: array
          items:
            type: string
        assignee:
          type: string
          nullable: true
          description: Name or email of the person assigned to this task

    UpdateTask:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        status:
          type: string
          enum: [todo, in_progress, in_review, done]
        priority:
          type: string
          enum: [low, medium, high, critical]
        due_date:
          type: string
          format: date
          nullable: true
        parent:
          type: integer
          nullable: true
        labels:
          type: array
          items:
            type: string
        archived:
          type: boolean
        assignee:
          type: string
          nullable: true
          description: Name or email of the person assigned to this task

    # --- Saúde ---
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          example: ok
        version:
          type: string
          example: "0.22.1"

    # --- Auth ---
    LoginUsuario:
      type: object
      required: [usuario, senha]
      properties:
        usuario:
          type: string
          example: yuri
        senha:
          type: string
          format: password

    CriarUsuario:
      type: object
      required: [usuario, nome, senha]
      properties:
        usuario:
          type: string
          minLength: 3
          maxLength: 30
          pattern: "^[a-z0-9_]+$"
          example: carlos
        nome:
          type: string
          minLength: 2
          example: Carlos Silva
        senha:
          type: string
          format: password
          minLength: 8

    LoginResponse:
      type: object
      properties:
        token:
          type: string
        usuario:
          $ref: "#/components/schemas/UsuarioResumo"

    UsuarioResumo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        usuario:
          type: string
        nome:
          type: string
        papel:
          type: string
          enum: [admin, editor, membro]

    # --- Usuário ---
    Usuario:
      type: object
      properties:
        id:
          type: string
          format: uuid
        usuario:
          type: string
        nome:
          type: string
        papel:
          type: string
          enum: [admin, editor, membro]
        bio:
          type: string
          nullable: true
        foto_url:
          type: string
          nullable: true
        criado_em:
          type: string
          format: date-time
        atualizado_em:
          type: string
          format: date-time

    Membro:
      type: object
      properties:
        id:
          type: string
        usuario:
          type: string
        nome:
          type: string
        bio:
          type: string
          nullable: true
        foto_url:
          type: string
          nullable: true
        criado_em:
          type: string
          format: date-time

    AtualizarPerfil:
      type: object
      properties:
        nome:
          type: string
        bio:
          type: string
        foto_url:
          type: string

    # --- Evento ---
    Evento:
      type: object
      properties:
        id:
          type: integer
        titulo:
          type: string
        data:
          type: string
          format: date
        hora:
          type: string
          example: "14:00"
        local:
          type: string
        descricao_md:
          type: string
        criado_por:
          type: string
          nullable: true
        criado_em:
          type: string
          format: date-time

    CriarEvento:
      type: object
      required: [titulo, data, hora, local]
      properties:
        titulo:
          type: string
        data:
          type: string
          format: date
        hora:
          type: string
        local:
          type: string
        descricao_md:
          type: string

    AtualizarEvento:
      type: object
      properties:
        titulo:
          type: string
        data:
          type: string
        hora:
          type: string
        local:
          type: string
        descricao_md:
          type: string

    # --- Missão ---
    Missao:
      type: object
      properties:
        id:
          type: integer
        titulo:
          type: string
        descricao:
          type: string
        objetivo:
          type: string
        status:
          type: string
          enum: [aberta, em_andamento, concluida, cancelada]
        criado_por:
          type: string
          nullable: true
        criado_em:
          type: string
          format: date-time
        atualizado_em:
          type: string
          format: date-time
        participantes:
          type: integer
          description: Contagem de participantes aprovados

    CriarMissao:
      type: object
      required: [titulo]
      properties:
        titulo:
          type: string
        descricao:
          type: string
        objetivo:
          type: string

    Participacao:
      type: object
      properties:
        id:
          type: integer
        missao_id:
          type: integer
        usuario_id:
          type: string
        status:
          type: string
          enum: [pendente, aprovado, recusado]
        criado_em:
          type: string
          format: date-time

    AtualizarParticipacao:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [pendente, aprovado, recusado]

    # --- Comentário ---
    Comentario:
      type: object
      properties:
        id:
          type: integer
        publicacao_slug:
          type: string
          nullable: true
        evento_id:
          type: integer
          nullable: true
        autor_id:
          type: string
          nullable: true
        nome_exibicao:
          type: string
          nullable: true
        conteudo:
          type: string
        anonimo:
          type: boolean
        criado_em:
          type: string
          format: date-time

    CriarComentario:
      type: object
      required: [conteudo]
      properties:
        publicacao_slug:
          type: string
        evento_id:
          type: integer
        nome_exibicao:
          type: string
        conteudo:
          type: string
          maxLength: 2000
        anonimo:
          type: boolean
          default: true

    # --- Mensagem ---
    Mensagem:
      type: object
      properties:
        id:
          type: integer
        remetente_id:
          type: string
          nullable: true
        destinatario_id:
          type: string
          nullable: true
        missao_id:
          type: integer
          nullable: true
        assunto:
          type: string
        conteudo:
          type: string
        nome_remetente:
          type: string
          nullable: true
        email_remetente:
          type: string
          nullable: true
        lida:
          type: boolean
        criado_em:
          type: string
          format: date-time

    CriarMensagem:
      type: object
      required: [conteudo]
      properties:
        destinatario_id:
          type: string
        missao_id:
          type: integer
        assunto:
          type: string
        conteudo:
          type: string
          maxLength: 5000
        nome_remetente:
          type: string
        email_remetente:
          type: string

    # --- Atividade ---
    Atividade:
      type: object
      properties:
        id:
          type: integer
        acao:
          type: string
        entidade:
          type: string
        entidade_id:
          type: string
          nullable: true
        usuario_id:
          type: string
          nullable: true
        conteudo:
          type: string
          nullable: true
        criado_em:
          type: string
          format: date-time

    # --- Gestão ---
    PublicarRequest:
      type: object
      required: [caminho, conteudo]
      properties:
        caminho:
          type: string
          description: "Caminho relativo no universo (ex: relatos/meu-post.md)"
          example: relatos/nova-horta.md
        conteudo:
          type: string
          description: Conteúdo completo do arquivo markdown com frontmatter YAML

    PublicarResponse:
      type: object
      properties:
        caminho:
          type: string
        tipo:
          type: string
        id:
          type: string

    ValidarResponse:
      type: object
      properties:
        valido:
          type: boolean
        erros:
          type: array
          items:
            type: string
        avisos:
          type: array
          items:
            type: string

    CriarConteudo:
      type: object
      required: [id, titulo]
      properties:
        id:
          type: string
          pattern: "^[a-zA-Z0-9._-]+$"
          example: nova-horta
        titulo:
          type: string
          example: Nova Horta Comunitária
        descricao:
          type: string
        data:
          type: string
          format: date
        tags:
          type: array
          items:
            type: string
        conteudo:
          type: string
          description: Corpo em markdown (abaixo do frontmatter)
        hora:
          type: string
          description: Apenas para eventos (HH:MM)
        local:
          type: string
          description: Apenas para eventos
        nome:
          type: string
          description: Apenas para membros
        papel:
          type: string
          description: Apenas para membros
          enum: [admin, editor, membro]
        status:
          type: string
          description: Apenas para quadro
        prioridade:
          type: string
          description: Apenas para quadro
        objetivo:
          type: string
          description: Apenas para quadro
        publicado:
          type: boolean
          description: Apenas para relatos

    AtualizarConteudo:
      type: object
      properties:
        titulo:
          type: string
        descricao:
          type: string
        data:
          type: string
        tags:
          type: array
          items:
            type: string
        conteudo:
          type: string
        hora:
          type: string
        local:
          type: string
        nome:
          type: string
        papel:
          type: string
        status:
          type: string
        prioridade:
          type: string
        objetivo:
          type: string
        publicado:
          type: boolean

    ConteudoResumo:
      type: object
      properties:
        id:
          type: string
        tipo:
          type: string
        titulo:
          type: string
        caminho:
          type: string

    # --- Iceberg ---
    Manifest:
      type: object
      properties:
        format_version:
          type: integer
        snapshot_id:
          type: string
          nullable: true
        entries:
          type: array
          items:
            $ref: "#/components/schemas/ManifestEntry"

    ManifestEntry:
      type: object
      properties:
        path:
          type: string
        schema_id:
          type: integer
        partition:
          type: object
          additionalProperties: true
        stats:
          type: object
          additionalProperties: true

